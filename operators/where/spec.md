## 算子需求规范

### 1. 基础信息
- **算子名称**: where
- **算子分类**: comparison  <!-- element-wise / reduction / matmul / attention / custom -->
- **数学公式**: $out_i = condition_i \ ? \ x_i \ : \ y_i$
- **功能描述**: 根据条件张量从 x 或 y 中选择元素。当 condition 对应位置为 True 时选择 x 的元素，为 False 时选择 y 的元素。输出张量与 x、y 具有相同的 shape。

### 2. 关键特性
<!-- 简单算子，无需复杂特性配置 -->

| 特性 | 是否需要 | 置信度 | 实现说明 | 优先级 |
|------|----------|--------|----------|--------|
| 广播支持 | ✓ 需要 | ✓ 高 | 支持 condition/x/y 之间的广播 | P1 |
| 标量支持 | ✓ 需要 | ✓ 高 | y 参数支持标量输入 | P2 |
| 动态轴 | ✓ 需要 | ✓ 高 | 支持 batch、seq_len 等动态维度 | P0 |

### 3. 算法描述
<!-- 简单 element-wise 算子，公式已足够描述计算逻辑 -->

逐元素条件选择操作，无需复杂算法描述。

### 4. 数据流图

```
    condition          输入 x            输入 y
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  [b, s, n, d] │  │  [b, s, n, d] │  │  [b, s, n, d] │
│    bool       │  │   float32     │  │   float32     │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       └────────┬────────┴────────┬────────┘
                │                 │
                ▼                 ▼
         ┌─────────────────────────────┐
         │      torch.where            │
         │  output[i] = cond[i] ? x[i] : y[i]
         └──────────────┬──────────────┘
                        │
                        ▼
               ┌──────────────┐
               │    输出 out   │
               │  [b, s, n, d] │
               │   float32     │
               └──────────────┘

公式: out_i = condition_i ? x_i : y_i
动态轴: b (batch), s (seq_len)
```

### 5. 数据规格

**输入规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| condition | [b, s, n, d] | bool | b, s | 条件张量，决定选择 x 还是 y |
| x | [b, s, n, d] | float32 | b, s | condition 为 True 时选择的值 |
| y | [b, s, n, d] 或 标量 | float32 | b, s | condition 为 False 时选择的值 |

**输出规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| out | [b, s, n, d] | float32 | b, s | 根据 condition 从 x 和 y 选择的元素 |

**广播规则**:
- condition, x, y 支持广播到相同的 shape
- 常见广播场景:
  - condition shape 与 x/y 不同但可广播（如 condition=[b,s,1,1], x=[b,s,n,d]）
  - y 为标量值

### 6. 数据类型支持

| Dtype | 支持 | atol | rtol | 备注 |
|-------|------|------|------|------|
| float32 | ✓ | 0.001 | 0.001 | 默认支持 |
| float16 | ✓ | 0.001 | 0.001 | 半精度支持 |
| bfloat16 | ✓ | 0.01 | 0.01 | BF16 支持 |
| int32 | ✓ | 0 | 0 | 整型支持 |

### 7. 精度要求
- **atol**: 0.001
- **rtol**: 0.001

### 8. 动态轴说明
- **动态轴**: b (batch), s (seq_len)
- **轴含义**:
  - b: batch 维度，批次大小
  - s: seq_len 维度，序列长度
  - n: head 维度，注意力头数（可选动态）
  - d: hidden_dim 维度，隐藏层维度
- **取值范围**: [1, INT32_MAX]

### 9. 边界条件处理
- **零值**: normal (正常计算，0 也是有效输入)
- **极值**: normal (正常计算，支持 float32 范围内的极值)
- **NaN/Inf**: normal (正常传递，不特殊处理)

### 10. 性能要求
- **性能目标**: 首跑精度成功性能的2倍

### 11. 参考信息
- **参考实现**: PyTorch torch.where(condition, input, other)
- **论文**: N/A (基础算子)
- **类似算子**: torch.masked_fill, torch.masked_select

### 12. 应用场景
- **目标模型**: Transformer, LLM
- **使用位置**: 注意力掩码处理、条件计算、混合专家路由

**典型配置**（用于 golden 验证和设计方案）:

| 配置名称 | 类型 | 优先级 | 参数 | 输入 Shape | 输出 Shape | 说明 |
|----------|------|--------|------|------------|------------|------|
| 性能_P0 | 性能 | P0 | 无特殊参数 | condition:[b,s,1,1], x:[b,s,n,d], y:[b,s,n,d] | [b,s,n,d] | 广播场景性能测试 |
| 功能_P0 | 功能 | P0 | 无特殊参数 | condition:[b,s,n,d], x:[b,s,n,d], y:[b,s,n,d] | [b,s,n,d] | 基础功能验证 |
| 功能_P1 | 功能 | P1 | y为标量 | condition:[b,s,n,d], x:[b,s,n,d], y:scalar | [b,s,n,d] | 标量 y 支持 |
| 动态轴 | 功能 | P1 | 动态 b,s | condition:[b,s,n,d], x:[b,s,n,d], y:[b,s,n,d] | [b,s,n,d] | 动态轴验证 |

---
*生成时间: 2026-03-28T00:00:00Z*
*确认状态: 已确认 (自动化流程)*
