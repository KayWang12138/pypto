## 算子需求规范

### 1. 基础信息
- **算子名称**: batch_matmul
- **算子分类**: matmul
- **数学公式**: $C[b,m,n] = \sum_{k} A[b,m,k] \times B[b,k,n]$
- **功能描述**: 批量矩阵乘法，对 batch 维度的每个矩阵执行矩阵乘法。支持可选的转置配置。

### 2. 关键特性
<!-- 简单算子，无需填写 -->

### 3. 算法描述
<!-- 简单算子，公式足以描述计算逻辑，无需算法描述 -->

### 4. 数据流图

```
      输入 x1              输入 x2
   ┌────────────┐     ┌────────────┐
   │ [b, m, k]  │     │ [b, k, n]  │
   └─────┬──────┘     └─────┬──────┘
         │                  │
         │    (可选transpose)│
         │                  │
         ▼                  ▼
    ┌───────────────────────────┐
    │      Batch Matmul         │
    │   C[b,m,n] = A[b,m,k]     │
    │            @ B[b,k,n]     │
    └─────────────┬─────────────┘
                  │
                  ▼
            ┌────────────┐
            │ 输出 y      │
            │ [b, m, n]  │
            └────────────┘

公式: y[b,i,j] = sum_k(x1[b,i,k] * x2[b,k,j])  for all b, i, j
动态轴: batch, m, k, n
```

### 5. 数据规格

**输入规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| x1 | [m, k] | float32 | 无 | 第一个输入矩阵 |
| x2 | [k, n] | float32 | 无 | 第二个输入矩阵 |

**可选参数**:

| 参数名 | 类型 | 默认值 | 说明 | 优先级 |
|--------|------|--------|------|--------|
| transpose_x1 | bool | False | 是否对 x1 转置。若 True，x1 shape 为 [k, m] | P2 |
| transpose_x2 | bool | False | 是否对 x2 转置。若 True，x2 shape 为 [n, k] | P2 |

**输出规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| y | [m, n] | float32 | 无 | 输出矩阵 |

### 6. 数据类型支持

| Dtype | 支持 | atol | rtol | 备注 |
|-------|------|------|------|------|
| float32 | Yes | 1e-5 | 1e-5 | 默认，与 torch.bmm 对比 |
| float16 | Yes | 1e-3 | 1e-3 | 可选支持 |
| bfloat16 | Yes | 1e-2 | 1e-2 | 可选支持 |

### 7. 精度要求
- **atol**: 1e-5
- **rtol**: 1e-5

### 8. 动态轴说明
- **动态轴**: batch, m, k, n
- **轴含义**:
  - batch: 批次大小
  - m: 输出矩阵行数 / x1 的行数
  - k: 矩阵乘法的内积维度
  - n: 输出矩阵列数 / x2 的列数
- **取值范围**: [1, INT32_MAX]

### 9. 边界条件处理
- **零值**: 正常计算
- **极值**: 正常计算
- **NaN/Inf**: 正常计算，不特殊处理

### 10. 性能要求
- **性能目标**: 首跑精度成功性能的2倍

### 11. 参考信息
- **参考实现**: torch.bmm (PyTorch), torch.matmul
- **论文**: N/A
- **类似算子**: matmul, addmm, linear

### 12. 应用场景
- **目标模型**: Transformer, BERT, GPT 等大语言模型
- **使用位置**: 注意力机制中的 Q@K^T, Scores@V，FFN 层

**典型配置**:

| 配置名称 | 类型 | 优先级 | 参数 | 输入 Shape | 输出 Shape | 说明 |
|----------|------|--------|------|------------|------------|------|
| 性能_P0 | 性能 | P0 | transpose_x1=False, transpose_x2=False | x1:[1,4096,4096], x2:[1,4096,4096] | y:[1,4096,4096] | 大规模矩阵乘法性能场景 |
| 功能_P0 | 功能 | P0 | transpose_x1=False, transpose_x2=False | x1:[4,1024,1024], x2:[4,1024,1024] | y:[4,1024,1024] | 标准功能验证 |
| 功能_P1 | 功能 | P1 | transpose_x1=False, transpose_x2=True | x1:[2,512,256], x2:[2,128,256] | y:[2,512,128] | x2转置场景 |
| 功能_P2 | 功能 | P2 | transpose_x1=True, transpose_x2=False | x1:[2,256,512], x2:[2,256,128] | y:[2,512,128] | x1转置场景 |

---
*生成时间: 2026-03-29*
*确认状态: 已确认 (非交互模式自动生成)*
