## 算子需求规范

### 1. 基础信息
- **算子名称**: matmul
- **算子分类**: matmul
- **数学公式**: $C = A @ B, \quad C_{ij} = \sum_k A_{ik} \cdot B_{kj}$
- **功能描述**: 矩阵乘法算子，支持 batch 维度广播的批量矩阵乘法。计算两个张量的矩阵乘积，其中最后两个维度执行标准矩阵乘法，前面的维度作为 batch 维度并支持广播。

### 2. 关键特性

| 特性 | 是否需要 | 置信度 | 实现说明 | 优先级 |
|------|----------|--------|----------|--------|
| batch_matmul | 需要 | 高 | 核心功能，支持 3D+ 张量的批量矩阵乘法 | P0 |
| dynamic_axis | 需要 | 高 | 支持 batch、M、N、K 维度动态变化 | P0 |
| batch_broadcast | 需要 | 高 | batch 维度支持广播（如 [B,1,M,K] @ [1,B,K,N] -> [B,B,M,N]） | P1 |
| 2D_matmul | 需要 | 高 | 支持标准 2D 矩阵乘法 [M,K] @ [K,N] -> [M,N] | P0 |
| 1D_vector | 不需要 | 高 | 不支持 1D 向量点积，简化实现范围 | P3 |
| sparse_support | 不需要 | 高 | 不支持稀疏矩阵，简化实现范围 | P3 |

### 3. 算法描述

```
Algorithm: Batched Matrix Multiplication
────────────────────────────────────────
输入: A [..., M, K], B [..., K, N]
输出: C [..., M, N]

1. 解析输入 shape:
   1.1 提取 A 的最后两维: M, K
   1.2 提取 B 的最后两维: K, N
   1.3 对齐 batch 维度并计算广播后的 batch shape

2. 广播 batch 维度:
   2.1 比较 A 和 B 的 batch 维度
   2.2 将维度为 1 的轴扩展到对应大小
   2.3 确定输出 batch shape

3. 执行矩阵乘法:
   3.1 for each batch index (b1, b2, ...):
       3.1.1 C[b1,b2,...,:,:] = A[b1,b2,...,:,:] @ B[b1,b2,...,:,:]
       3.1.2 即 C[...,i,j] = sum_k(A[...,i,k] * B[...,k,j])

4. return C
```

### 4. 数据流图

```
    输入 A                    输入 B
+-----------------+      +-----------------+
| [..., M, K]     |      | [..., K, N]     |
| float32/float16 |      | float32/float16 |
+--------+--------+      +--------+--------+
         |                        |
         |   +--------------------+
         v   v
    +----------------+
    |  C = A @ B     |
    | (batch matmul) |
    +-------+--------+
            v
      +-----------------+
      | 输出 C          |
      | [..., M, N]     |
      | float32/float16 |
      +-----------------+

动态轴: batch (可广播), M, N, K
约束: A 的 K 维度必须等于 B 的 K 维度
```

### 5. 数据规格

**输入规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| A | [..., M, K] | float32, float16 | batch, M, K | 左操作数，最后两维为 MxK 矩阵 |
| B | [..., K, N] | float32, float16 | batch, K, N | 右操作数，最后两维为 KxN 矩阵 |

**输出规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| C | [..., M, N] | float32, float16 | batch, M, N | 输出矩阵，最后两维为 MxN |

### 6. 数据类型支持

| Dtype | 支持 | atol | rtol | 备注 |
|-------|------|------|------|------|
| float32 | Yes | 0.001 | 0.001 | 默认，高精度 |
| float16 | Yes | 0.01 | 0.01 | 混合精度场景 |

### 7. 精度要求
- **atol**: 0.001 (float32) / 0.01 (float16)
- **rtol**: 0.001 (float32) / 0.01 (float16)

### 8. 动态轴说明
- **动态轴**: batch (所有前导维度), M, N, K
- **轴含义**:
  - batch: 批量大小，支持多维度和广播
  - M: 左矩阵的行数
  - N: 右矩阵的列数
  - K: 收缩维度（A 的列数 = B 的行数）
- **取值范围**:
  - batch: [1, 65535] 每个维度
  - M, N, K: [1, 65535]

### 9. 边界条件处理
- **零值**: 正常计算（0 乘以任何数等于 0）
- **极值**: 正常计算（可能出现 inf，由输入决定）
- **NaN/Inf**: 正常传播（不特殊处理）

### 10. 性能要求
- **性能目标**: 首跑精度成功性能的 2 倍

### 11. 参考信息
- **参考实现**: torch.matmul (PyTorch), numpy.matmul (NumPy)
- **论文**: 无
- **类似算子**: torch.mm (仅 2D), torch.bmm (仅 3D), torch.einsum

### 12. 应用场景
- **目标模型**: Transformer, ResNet, BERT, GPT 等几乎所有深度学习模型
- **使用位置**: Linear 层、Attention 计算、全连接层

**典型配置**:

| 配置名称 | 类型 | 优先级 | 参数 | 输入 Shape | 输出 Shape | 说明 |
|----------|------|--------|------|------------|------------|------|
| 性能_P0 | 性能 | P0 | - | A:[1024,4096,4096], B:[1024,4096,4096] | C:[1024,4096,4096] | 大规模 batch matmul，性能关键场景 |
| 功能_P0 | 功能 | P0 | - | A:[1,128,64,512], B:[1,128,512,256] | C:[1,128,64,256] | 典型 Attention QK^T 场景 |
| 广播_P1 | 功能 | P1 | - | A:[8,1,1024,4096], B:[1,4,4096,1024] | C:[8,4,1024,1024] | batch 广播场景 |
| 2D_P0 | 功能 | P0 | - | A:[4096,4096], B:[4096,4096] | C:[4096,4096] | 标准 2D 矩阵乘法 |
| 动态_M | 功能 | P0 | - | A:[B,M,K], B:[B,K,N] (动态) | C:[B,M,N] | 动态 shape 验证 |

---
*生成时间: 2026-03-28T23:10:00Z*
*确认状态: 自动确认（非交互模式）*
