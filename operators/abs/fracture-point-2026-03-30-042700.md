# PyPTO 断裂点检测报告

> **检测时间**: 2026-03-30T04:27:00Z
> **算子名称**: abs
> **Session ID**: 3ad5e32f-5c51-4482-a3e1-283f008ab63e
> **最终状态**: 成功 (6/6 测试通过)

---

## 环境信息

| 信息 | 值 |
|------|-----|
| CANN 版本 | 8.5.0 |
| PyPTO Commit | 2cc92d8a (2026-03-02) |
| 服务器类型 | Ascend A3 |
| Python 版本 | 3.10.12 |
| 操作系统 | Ubuntu 22.04.5 LTS |

---

## 检测到的断裂点

### FP-1: Orchestrator Agent 意外停止

| 字段 | 值 |
|------|-----|
| **类型** | C5 (任务中断) |
| **实体** | pypto-op-orchestrator Agent |
| **优先级** | P1 (高) |
| **根因归属** | skill |
| **置信度** | 高 |

**问题描述**:
Orchestrator Agent 在完成 Stage 3 (Golden 生成) 后意外停止，未继续执行 Stage 4-6。

**证据片段**:
```
## Stage 3 完成
...
**门禁状态**: PASS
...
现在返回给 Orchestrator，继续推进到 **Stage 4: Design 设计**。
```
[Agent 输出终止，未继续执行]

**归因分析**:
```json
{
  "root_cause": "skill",
  "confidence": "high",
  "evidence": "Agent 在 Stage 3 完成后输出 '继续推进到 Stage 4' 但实际未执行，可能是网络超时或 Agent 状态管理问题"
}
```

**优化建议**:
1. 在 `pypto-op-orchestrator` 中添加心跳检测和自动恢复机制
2. 在状态文件中记录每个 stage 的完成时间戳，便于断点续传
3. 考虑将 Stage 4-6 作为独立的 subagent 调用，避免单 agent 超时

---

### FP-2: 4D Tiling 配置导致编译失败

| 字段 | 值 |
|------|-----|
| **类型** | A1 (API 约束未文档化) |
| **实体** | `pypto.set_vec_tile_shapes` |
| **优先级** | P1 (高) |
| **根因归属** | framework |
| **置信度** | 高 |

**问题描述**:
使用 `pypto.set_vec_tile_shapes(4, 16, 64, 128)` 配置 4D tiling 时，编译阶段报错 "Run pass failed"。框架对 4D tiling 的约束未在文档中明确说明。

**证据片段**:
```
[INTERNAL BUG] /workspace/code/pypto/operators/abs/abs_impl.py:1:0: Errcode: FFFFFF!
Run pass failed., func CompileFunction, file host_machine.cpp, line 179
```

**修复方案**:
将 4D 输入 reshape 到 2D，使用 `pypto.set_vec_tile_shapes(64, 128)` 处理。

**归因分析**:
```json
{
  "root_cause": "framework",
  "confidence": "high",
  "evidence": "4D tiling 配置 (4, 16, 64, 128) 导致 Run pass failed，但文档未说明 4D tiling 的具体约束"
}
```

**优化建议**:
1. 在 `docs/api/config/pypto-set_vec_tile_shapes.md` 中补充 4D tiling 的约束说明
2. 建议在 examples 中添加 4D 输入的标准处理模式（reshape 到 2D）
3. 考虑在 API 层面提供 4D→2D reshape 的自动处理

---

## 统计信息

| 指标 | 值 |
|------|-----|
| **断裂点总数** | 2 |
| **致命 (P0)** | 0 |
| **高 (P1)** | 2 |
| **中 (P2)** | 0 |
| **fps_total** | 2 |
| **fps_confirmed** | 2 |

---

## 关联断裂点

| 断裂点 | 关联断裂点 |
|--------|------------|
| FP-1 | - |
| FP-2 | - |

---

## Session 操作统计

| 操作类型 | 次数 |
|----------|------|
| 文件读取 (Read) | ~15 |
| 文件写入 (Write) | 6 |
| Bash 命令 | ~20 |
| Grep 搜索 | 1 |
| Agent 调用 | 1 |

---

## 涉及实体

| 实体 | 类型 | 复杂度 | 相关断裂点 |
|------|------|--------|------------|
| pypto-op-orchestrator | Agent | 中等 | FP-1 |
| pypto.set_vec_tile_shapes | API | 简单 | FP-2 |
| pypto.abs | API | 简单 | - |

---

*报告生成时间: 2026-03-30T04:27:00Z*
