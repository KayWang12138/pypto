## 算子需求规范

### 1. 基础信息
- **算子名称**: clip
- **算子分类**: element-wise  <!-- 逐元素操作 -->
- **数学公式**: $y = \min(\max(x, \text{min\_val}), \text{max\_val})$
- **功能描述**: 将输入张量的每个元素限制在 [min_val, max_val] 范围内。如果 x < min_val，输出 min_val；如果 x > max_val，输出 max_val；否则输出 x。

### 2. 关键特性
<!-- 简单算子，无复杂特性 -->

| 特性 | 是否需要 | 置信度 | 实现说明 | 优先级 |
|------|----------|--------|----------|--------|
| 动态轴支持 | ✓ 需要 | ✓ 高 | 所有维度支持动态shape | P0 |
| 标量 min/max | ✓ 需要 | ✓ 高 | min/max 为标量 | P0 |
| 张量 min/max | ✗ 不需要 | ✓ 高 | 暂不支持广播形式 | P3 |

### 3. 算法描述
<!-- 简单逐元素操作，无需算法描述 -->

### 4. 数据流图

```
    输入 x              输入 min_val       输入 max_val
┌──────────────┐   ┌─────────┐       ┌─────────┐
│ [*, dynamic] │   │ scalar  │       │ scalar  │
│   float32    │   │ float32 │       │ float32 │
└──────┬───────┘   └────┬────┘       └────┬────┘
       │                │                 │
       │                │                 │
       ▼                ▼                 ▼
    ┌─────────────────────────────────────────────┐
    │            y = clip(x, min, max)            │
    │    if x < min: min                          │
    │    elif x > max: max                        │
    │    else: x                                  │
    └─────────────────────┬───────────────────────┘
                          │
                          ▼
                   ┌──────────────┐
                   │ 输出 y        │
                   │ [*, dynamic] │
                   │   float32    │
                   └──────────────┘

公式: y = min(max(x, min_val), max_val)
动态轴: 所有维度
```

### 5. 数据规格

**输入规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| x | [*, dynamic] | float32 | 所有维度 | 输入张量，支持任意维度 |
| min_val | scalar | float32 | 无 | 最小值边界 |
| max_val | scalar | float32 | 无 | 最大值边界 |

**输出规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| y | [*, dynamic] | float32 | 所有维度 | 输出张量，shape与输入x相同 |

### 6. 数据类型支持

| Dtype | 支持 | atol | rtol | 备注 |
|-------|------|------|------|------|
| float32 | ✓ | 0.001 | 0.001 | 默认 |
| float16 | ✓ | 0.01 | 0.01 | 可选 |
| bfloat16 | ✓ | 0.01 | 0.01 | 可选 |

### 7. 精度要求
- **atol**: 0.001
- **rtol**: 0.001

### 8. 动态轴说明
- **动态轴**: 所有维度
- **轴含义**: 支持任意维度的动态变化
- **取值范围**: [1, INT32_MAX]

### 9. 边界条件处理
- **零值**: normal (正常计算)
- **极值**: normal (正常计算)
- **NaN/Inf**: normal (正常计算，保持不变)

### 10. 性能要求
- **性能目标**: 首跑精度成功性能的2倍

### 11. 参考信息
- **参考实现**: PyTorch torch.clamp(input, min=None, max=None)
- **论文**: 无
- **类似算子**: torch.clamp, numpy.clip, tensorflow.clip_by_value

### 12. 应用场景
- **目标模型**: 通用
- **使用位置**: 激活函数后处理、梯度裁剪、数值范围限制

**典型配置**（用于 golden 验证和设计方案）:

| 配置名称 | 类型 | 优先级 | 参数 | 输入 Shape | 输出 Shape | 说明 |
|----------|------|--------|------|------------|------------|------|
| 性能_P0 | 性能 | P0 | min=-1.0, max=1.0 | x: [1024, 1024] | y: [1024, 1024] | 核心性能场景 |
| 功能_P0 | 功能 | P0 | min=-1.0, max=1.0 | x: [32, 64, 128] | y: [32, 64, 128] | 核心功能验证 |
| 动态_P0 | 功能 | P0 | min=0.0, max=1.0 | x: [batch, seq, hidden] | y: [batch, seq, hidden] | 动态轴验证 |

---
*生成时间: 2026-03-29T00:00:00Z*
*确认状态: 已确认*
