# PyPTO 断裂点检测报告

> **生成时间**: 2026-03-29 13:35:00
> **算子**: sigmoid
> **Session**: sigmoid 算子端到端开发

---

## 执行摘要

- **算子名称**: sigmoid
- **开发阶段**: Stage 1-7 全部完成
- **最终状态**: SUCCESS
- **测试结果**: 全部通过 (FP16/FP32/BF16)
- **断裂点数量**: fps_total=1, fps_confirmed=0

---

## 1. 概述
本次 session 成功完成了 sigmoid 算子的端到端开发，所有测试用例均通过。检测到 1 个潜在的文档类断裂点，需要进一步确认。

---

## 2. 断裂点详情

### 2.1 文档类断裂点 (D类)

| ID | 类型 | 优先级 | 实体 | 置信度 | 状态 |
|----|------|--------|------|--------|------|
| FP-1 | D2 | 中 | pypto.sigmoid | 中 | 待确认 |

#### FP-1: pypto.sigmoid dtype 支持文档不完整

**问题描述**:
`pypto.sigmoid` API 文档 (`docs/api/operation/pypto-sigmoid.md`) 声明仅支持 `DT_FP32`，但实际测试表明该 API 也支持 `DT_FP16` 和 `DT_BF16`。

**根因归属**: 文档缺失/不完整

**证据片段**:

1. API 文档声明 (docs/api/operation/pypto-sigmoid.md):
   > Tensor支持的数据类型为：DT_FP32。

2. 实际测试输出 (test_sigmoid.py):
   ```
   dtype: torch.float16, Max diff: 0.000000e+00
   [PRECISION_PASS]
   dtype: torch.bfloat16, Max diff: 0.000000e+00
   [PRECISION_PASS]
   ```

**影响范围**:
- 用户可能因文档限制而不敢使用 FP16/BF16
- 导致不必要的组合实现方案

**优化建议**:
1. 更新 `docs/api/operation/pypto-sigmoid.md` 文档，添加 FP16/BF16 支持说明
2. 在文档中添加 dtype 支持矩阵表格，与其他 API 文档格式统一
3. 参考 `pypto.exp` 文档格式，明确列出所有支持的 dtype

---

## 3. Session 级断裂点 (C类)

本次 session 未检测到 Session 级断裂点。

---

## 4. 统计

| 指标 | 数值 |
|------|------|
| fps_total | 1 |
| fps_confirmed | 0 |
| fps_high | 0 |
| fps_medium | 1 |
| fps_low | 0 |

---

## 5. 建议

1. **优先级 P0**: 确认 `pypto.sigmoid` 对 FP16/BF16 的支持是否为预期行为，如果是，更新文档
2. **优先级 P1**: 在 spec.md 中明确说明 dtype 支持范围，参考实际测试结果

---

*报告生成时间: 2026-03-29 13:35:00*
*检测工具: pypto-fracture-point-detector*
