## 算子需求规范

### 1. 基础信息
- **算子名称**: sigmoid
- **算子分类**: element-wise
- **数学公式**: $\sigma(x) = \frac{1}{1 + e^{-x}}$
- **功能描述**: Sigmoid 激活函数。将输入逐元素映射到 (0, 1) 区间，常用于二分类输出层和门控机制。

### 2. 关键特性
<!-- 简单 element-wise 算子，无复杂特性 -->

### 3. 算法描述
<!-- 简单 element-wise 算子，公式已完整描述计算逻辑 -->

逐元素计算：`y[i] = sigmoid(x[i]) = 1 / (1 + exp(-x[i]))`

### 4. 数据流图

```
    输入 x                    输出 y
┌──────────────────┐         ┌──────────────────┐
│  [m, n] 或       │         │  [m, n] 或       │
│  [b, m, n] 或    │ ──────▶ │  [b, m, n] 或    │
│  [b, s, m, n]    │ sigmoid │  [b, s, m, n]    │
│  float16/float32 │         │  float16/float32 │
│  /bfloat16       │         │  /bfloat16       │
└──────────────────┘         └──────────────────┘

公式: sigmoid(x) = 1 / (1 + exp(-x))
      输出范围: (0, 1)
约束: Shape仅支持2-4维，Shape Size <= INT32_MAX
      不支持空Tensor
```

### 5. 数据规格

**输入规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| x | [m, n] 或 [b, m, n] 或 [b, s, m, n] | DT_FP16/DT_FP32/DT_BF16 | 所有维度 | 输入张量，不支持空Tensor |

**输出规格**:

| 变量 | Shape | Dtype | 动态轴 | 说明 |
|------|-------|-------|--------|------|
| y | 与输入相同 | 与输入相同 | 所有维度 | 输出张量，shape和dtype与输入相同，值域(0, 1) |

### 6. 数据类型支持

| Dtype | 支持 | atol | rtol | 备注 |
|-------|------|------|------|------|
| float32 | Y | 3e-3 | 3e-3 | 默认 |
| float16 | Y | 3e-3 | 3e-3 | 常用 |
| bfloat16 | Y | 3e-3 | 3e-3 | 常用 |

### 7. 精度要求
- **atol**: 3e-3
- **rtol**: 3e-3

### 8. 动态轴说明
- **动态轴**: b, s (batch, seq)
- **轴含义**: b = batch size, s = sequence length, m/n = 特征维度
- **取值范围**: [1, INT32_MAX]，Shape Size不大于INT32_MAX

### 9. 边界条件处理
- **零值**: 正常计算，sigmoid(0) = 0.5
- **极值**: 正常计算，IEEE 754 标准处理
  - x -> +inf: y -> 1 (趋近于1)
  - x -> -inf: y -> 0 (趋近于0)
- **NaN/Inf**: 按 IEEE 754 标准传播

### 10. 性能要求
- **性能目标**: 首跑精度成功性能的2倍

### 11. 参考信息
- **参考实现**: PyTorch `torch.sigmoid` / `torch.nn.functional.sigmoid`
- **论文**: N/A
- **类似算子**: tanh, relu, silu, gelu

### 12. 应用场景
- **目标模型**: 二分类网络、LSTM、Transformer、GAN
- **使用位置**: 输出层、门控机制、注意力权重

**典型配置**:

| 配置名称 | 类型 | 优先级 | 参数 | 输入 Shape | 输出 Shape | 说明 |
|----------|------|--------|------|------------|------------|------|
| 性能_P0 | 性能 | P0 | - | [1024, 1024] | [1024, 1024] | 核心性能场景 |
| 功能_P0 | 功能 | P0 | - | [32, 64] | [32, 64] | 核心功能验证 |
| 功能_P1 | 功能 | P1 | - | [2, 128, 256] | [2, 128, 256] | 3维输入验证 |
| 功能_P2 | 功能 | P2 | - | [1, 1, 64, 64] | [1, 1, 64, 64] | 4维输入验证 |

---
*生成时间: 2026-03-29*
*确认状态: 已确认（自动模式）*
