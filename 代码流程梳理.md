# Hello World 示例代码流程梳理

本文档以 `hello_world.py` 为例，详细梳理 PyPTO 框架的完整代码执行流程。

## 一、整体架构概览

PyPTO 是一个将 Python 函数编译为 NPU 可执行代码的框架，主要包含三个层次：

1. **Python 前端层** (`python/pypto/`): 提供用户 API，处理 JIT 编译和运行时执行
2. **C++ 框架层** (`framework/`): 提供底层运行时和设备启动器
3. **示例层** (`examples/`): 用户代码示例

## 二、代码执行流程

### 阶段 1: 程序入口和初始化

**文件**: `examples/00_hello_world/hello_world.py`

```python
# 1. 命令行参数解析
main() -> argparse 解析 --run_mode, --list 等参数

# 2. 设备 ID 获取和验证
get_device_id() -> 从环境变量 TILE_FWK_DEVICE_ID 获取设备 ID

# 3. 运行模式设置
- NPU 模式: 需要 CANN 环境和 NPU 硬件
- SIM 模式: 模拟器模式，无需硬件
```

**关键代码路径**:
- `main()` (line 91-197)
- `get_device_id()` (line 25-44)

### 阶段 2: 内核函数定义和装饰

**文件**: `examples/00_hello_world/hello_world.py`

```python
@pypto.frontend.jit(runtime_options={"run_mode": mode})
def add_kernel(
    x: pypto.Tensor(shape, pypto.DT_FP32),
    y: pypto.Tensor(shape, pypto.DT_FP32),
) -> pypto.Tensor(shape, pypto.DT_FP32):
    pypto.set_vec_tile_shapes(1, 4, 1, 64)
    out = x + y
    return out
```

**装饰器处理流程**:

1. **装饰器应用** (`python/pypto/frontend/parser/entry.py`)
   - `@pypto.frontend.jit()` 被调用
   - 创建 `JitCallableWrapper` 对象（延迟编译模式）
   - 保存原始函数、配置选项等元数据
   - **此时不进行编译**，只是包装函数

2. **关键类**: `JitCallableWrapper`
   - `_original_func`: 原始 Python 函数
   - `_pto_function`: 编译后的 PTO 函数（初始为 None）
   - `_is_compiled`: 编译标志（初始为 False）
   - `_runtime_options`: 运行时选项（包含 run_mode）

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::jit()` (line 744-843)
- `python/pypto/frontend/parser/entry.py::JitCallableWrapper.__init__()` (line 115-250)

### 阶段 3: 内核函数调用（触发编译和执行）

**文件**: `examples/00_hello_world/hello_world.py`

```python
output_data = create_add_kernel(shape, run_mode)(input_data0, input_data1)
```

当用户调用装饰后的函数时，触发以下流程：

#### 3.1 输入张量转换

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def __call__(self, *args, **kwargs):
    # 1. 转换 torch.Tensor 为 pypto.Tensor
    in_tensors = [self._convert_torch_to_pto(arg) for arg in args]
    
    # 2. 提取设备信息
    device = in_tensors[0].device  # 例如: torch.device('npu:0')
```

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::JitCallableWrapper.__call__()` (line 251-355)

#### 3.2 延迟编译（Lazy Compilation）

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def _compile_if_needed(self, concrete_input_shapes):
    if self._is_compiled:
        return
    
    # 1. 创建解析器
    self._parser = self._create_parser()
    self._parser.parse()  # 解析 AST，进行活跃性分析
    
    # 2. 初始化后端
    pypto_impl.DeviceInit()
    handler = pypto_impl.OperatorBegin()
    
    # 3. 设置配置选项
    self._set_config_option()
    
    # 4. 绑定动态维度
    if concrete_input_shapes:
        self._parser.bind_dynamic_dims_from_inputs(concrete_input_shapes)
    
    # 5. 执行解析，生成 PTO IR
    self._pto_function = self._parser.execute()
    pypto_impl.OperatorEnd(handler)
    
    self._handler = handler
    self._is_compiled = True
```

**编译过程详解**:

1. **AST 解析** (`python/pypto/frontend/parser/parser.py`)
   - 将 Python 函数转换为 AST
   - 转换为 doc AST（中间表示）
   - 进行活跃性分析（liveness analysis）

2. **IR 生成** (`python/pypto/frontend/parser/parser.py`)
   - 访问 AST 节点，生成 PTO IR
   - 处理张量操作（如 `x + y`）
   - 创建 `pypto.Function` 对象

3. **后端编译** (C++ 层)
   - `OperatorBegin()`: 开始操作符编译
   - 代码生成、优化、生成二进制
   - `OperatorEnd()`: 结束编译，返回 handler

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::_compile_if_needed()` (line 485-532)
- `python/pypto/frontend/parser/parser.py::Parser.execute()` (line ~200-300)
- `python/pypto/frontend/parser/parser.py::Parser._visit_function_def()` (line 966-1017)

#### 3.3 输出张量分配

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def __call__(self, *args, **kwargs):
    # 从函数签名获取输出形状和类型
    out_tensors = self._allocate_output_tensors(in_tensors, device)
```

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::JitCallableWrapper.__call__()` (line 340-355)

#### 3.4 运行时执行分发

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def _dispatch_with_run_mode(self, in_tensors, out_tensors, device):
    # 1. 设置运行时调试模式
    self._set_runtime_debug_mode()
    
    # 2. 检查 CANN 环境
    cann_is_configed = bool(os.environ.get("ASCEND_HOME_PATH"))
    run_mode = pypto.get_runtime_options().get("run_mode", 0)
    
    # 3. 根据运行模式分发
    if run_mode == 0:  # NPU 模式
        if not cann_is_configed:
            raise RuntimeError("Please source cann environment...")
        self._run_with_npu(in_tensors, out_tensors, device)
    else:  # SIM 模式
        self._run_with_cpu(in_tensors, out_tensors)
```

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::_dispatch_with_run_mode()` (line 650-686)

### 阶段 4: NPU 模式执行

#### 4.1 NPU 执行准备

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def _run_with_npu(self, in_tensors, out_tensors, device):
    # 1. 转换为设备张量数据
    in_tensor_data = _pto_to_tensor_data(in_tensors)
    out_tensor_data = _pto_to_tensor_data(out_tensors)
    
    # 2. 设备切换（如果需要）
    ori_device = torch.npu.current_device()
    if device.index != ori_device:
        torch.npu.set_device(device.index)
        self._run(in_tensor_data, out_tensor_data, device)
        torch.npu.set_device(ori_device)
    else:
        self._run(in_tensor_data, out_tensor_data, device)
```

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::_run_with_npu()` (line 577-616)

#### 4.2 工作空间分配和执行

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def _run(self, in_tensor_data, out_tensor_data, device):
    # 1. 获取工作空间大小
    workspace_size = pypto_impl.GetWorkSpaceSize(
        self._handler, in_tensor_data, out_tensor_data
    )
    
    # 2. 分配工作空间内存
    workspace_tensor = torch.empty(workspace_size, dtype=torch.uint8, device=device)
    
    # 3. 调用后端执行
    runtime_error_msg = pypto_impl.OperatorDeviceRunOnceDataFromDevice(
        self._handler,
        in_tensor_data + out_tensor_data,
        [],
        torch.npu.current_stream().npu_stream,
        workspace_tensor.data_ptr(),
    )
    
    if runtime_error_msg != "":
        raise RuntimeError(runtime_error_msg)
```

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::_run()` (line 534-575)

#### 4.3 C++ 层设备启动

**文件**: `framework/src/machine/runtime/device_launcher.cpp`

```cpp
int DeviceLauncher::DeviceLaunchOnceWithDeviceTensorData(
    Function *function,
    const std::vector<DeviceTensorData> &inputList,
    const std::vector<DeviceTensorData> &outputList,
    rtStream_t aicpuStream,
    rtStream_t aicoreStream,
    bool streamSynchronize,
    CachedOperator *cachedOperator,
    const DeviceLauncherConfig &config) {
    
    // 阶段 0: 函数开始
    // 阶段 1: 初始化和配置设置
    config::SetRunDataOption(KEY_RUNTYPE, "npu");
    
    // 阶段 2: 设置二进制数据
    DeviceRunner::SetBinData(function->GetDyndevAttribute()->kernelBinary);
    
    // 阶段 3: 设置捕获流
    SetCaptureStream(aicoreStream, aicpuStream, isCapture);
    
    // 阶段 4: 设置捕获模式和性能分析函数
    DeviceRunner::Get().SetCaptureFlag(isCapture);
    DeviceRunner::Get().GetHostProfInstance().SetProfFunction(function);
    
    // 阶段 5: ACL 初始化
    aclInit(nullptr);
    
    // 阶段 6: 缓存操作符处理
    // 阶段 7: 检查设备 ID
    CheckDeviceId();
    
    // 阶段 8: 初始化 Tiling 数据
    DeviceInitTilingData(DeviceMemoryUtils(), kArgs, 
                        function->GetDyndevAttribute()->devProgBinary, 
                        config, cachedOperator);
    
    // 阶段 9: 初始化内核输入输出
    DeviceInitKernelInOuts(DeviceMemoryUtils(), kArgs, 
                          inputList, outputList,
                          function->GetDyndevAttribute()->disableL2List, 
                          config.isGETensorList);
    
    // 阶段 10: 注册内核二进制
    DeviceRunner::Get().RegisterKernelBin(&binHandle);
    
    // 阶段 11: 动态启动
    DeviceRunner::Get().DynamicLaunch(aicpuStream, nullptr, aicoreStream, 
                                      0, &kArgs, config.blockdim, config.aicpuNum);
    
    // 阶段 12: 性能分析运行
    RunWithProfile(aicoreStream, aicpuStream);
    
    // 阶段 13: 流同步（如果启用）
    if (streamSynchronize) {
        DeviceRunner::Get().DynamicLaunchSynchronize(aicpuStream, nullptr, aicoreStream);
    }
    
    return rc;
}
```

**关键代码路径**:
- `framework/src/machine/runtime/device_launcher.cpp::DeviceLaunchOnceWithDeviceTensorData()` (line 126-262)

### 阶段 5: SIM 模式执行

**文件**: `python/pypto/frontend/parser/entry.py`

```python
def _run_with_cpu(self, in_tensors, out_tensors):
    """在 CPU 上使用成本模型模拟执行"""
    from .cost_model import _cost_model_run_once_data_from_host
    _cost_model_run_once_data_from_host(in_tensors, out_tensors)
```

SIM 模式通过成本模型在 CPU 上模拟执行，无需 NPU 硬件。

**关键代码路径**:
- `python/pypto/frontend/parser/entry.py::_run_with_cpu()` (line 618-634)
- `python/pypto/cost_model/` (成本模型实现)

### 阶段 6: 结果验证

**文件**: `examples/00_hello_world/hello_world.py`

```python
def test_add_direct(device_id=None, run_mode: str = "npu"):
    # 1. 准备输入数据
    input_data0 = torch.rand(shape, dtype=torch.float, device=device)
    input_data1 = torch.rand(shape, dtype=torch.float, device=device)
    
    # 2. 执行内核
    output_data = create_add_kernel(shape, run_mode)(input_data0, input_data1)
    
    # 3. 计算黄金值
    golden = torch.add(input_data0, input_data1)
    
    # 4. 比较结果
    max_diff = np.abs(output_data.cpu().numpy() - golden.cpu().numpy()).max()
    assert_allclose(np.array(output_data.cpu()), np.array(golden.cpu()), 
                    rtol=3e-3, atol=3e-3)
```

**关键代码路径**:
- `examples/00_hello_world/hello_world.py::test_add_direct()` (line 68-88)

## 三、关键数据流

### 3.1 张量数据流

```
torch.Tensor (用户输入)
    ↓
pypto.Tensor (前端转换)
    ↓
DeviceTensorData (C++ 层)
    ↓
NPU 设备内存
    ↓
执行结果
    ↓
torch.Tensor (返回给用户)
```

### 3.2 编译流程

```
Python 函数 (add_kernel)
    ↓
@pypto.frontend.jit 装饰器
    ↓
JitCallableWrapper (延迟编译)
    ↓
首次调用时:
    - AST 解析
    - doc AST 转换
    - 活跃性分析
    - IR 生成
    - 后端编译
    ↓
pypto.Function + handler
    ↓
可执行内核二进制
```

### 3.3 执行流程

```
用户调用 add_kernel(x, y)
    ↓
JitCallableWrapper.__call__()
    ↓
编译检查 → 如未编译则编译
    ↓
输出张量分配
    ↓
运行模式分发
    ├─ NPU 模式 → _run_with_npu() → DeviceLaunchOnceWithDeviceTensorData()
    └─ SIM 模式 → _run_with_cpu() → 成本模型模拟
    ↓
结果返回
```

## 四、关键类和接口

### 4.1 Python 层

| 类/函数 | 文件 | 功能 |
|---------|------|------|
| `JitCallableWrapper` | `python/pypto/frontend/parser/entry.py` | JIT 编译和执行包装器 |
| `Parser` | `python/pypto/frontend/parser/parser.py` | Python AST 到 PTO IR 的解析器 |
| `RunMode` | `python/pypto/runtime.py` | 运行模式枚举（NPU/SIM） |

### 4.2 C++ 层

| 类/函数 | 文件 | 功能 |
|---------|------|------|
| `DeviceLauncher` | `framework/src/machine/runtime/device_launcher.cpp` | 设备启动器，负责内核执行 |
| `DeviceRunner` | `framework/src/machine/runtime/device_runner.cpp` | 设备运行器，管理设备资源 |
| `Function` | `framework/` | PTO 函数表示，包含编译后的二进制 |

## 五、配置选项

### 5.1 运行时选项 (runtime_options)

- `run_mode`: `RunMode.NPU` 或 `RunMode.SIM`
- 其他运行时配置

### 5.2 代码生成选项 (codegen_options)

- 控制代码生成过程

### 5.3 主机选项 (host_options)

- 控制主机端行为

### 5.4 Pass 选项 (pass_options)

- 控制编译器优化 Pass

## 六、错误处理

1. **编译时错误**: 由 Parser 的 Diagnostics 系统报告
2. **运行时错误**: 由后端返回错误消息
3. **设备错误**: 检查 CANN 环境和设备可用性

## 七、性能优化特性

1. **延迟编译**: 首次调用时才编译，避免模块加载时的开销
2. **缓存机制**: 相同输入形状的编译结果可复用
3. **工作空间复用**: 工作空间内存可复用
4. **流异步执行**: 支持异步执行和流同步

## 八、总结

`hello_world.py` 示例展示了 PyPTO 框架的完整执行流程：

1. **定义阶段**: 使用 `@pypto.frontend.jit` 装饰器定义内核函数
2. **编译阶段**: 首次调用时进行延迟编译，将 Python 代码转换为 PTO IR 和可执行二进制
3. **执行阶段**: 根据运行模式（NPU/SIM）分发到相应的执行路径
4. **验证阶段**: 比较执行结果与黄金值

整个流程体现了 PyPTO 的设计理念：**用户友好的 Python API + 高效的底层执行**。
