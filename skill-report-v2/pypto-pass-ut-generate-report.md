# 技能评审报告

## 评审摘要

| 项目 | 结果 |
|------|------|
| 技能名称 | `pypto-pass-ut-generate` |
| 评审时间 | 2026-03-11 13:53:32 CST |
| 总分 | **92.35 / 100** |
| 等级 | **A** |
| S0 否决 | 否 |
| 规则统计 | PASS 37 / FAIL 11 / WARN 0 / SKIP 3 |

## 维度评分表

| 维度 | 原始分(0-100) | 权重 | 加权分 | 扣分明细（FAIL） |
|------|---------------|------|--------|------------------|
| D1 | 88 | 25% | 22.00 | R04(-10), R06(-2) |
| D2 | 95 | 15% | 14.25 | R14(-5) |
| D3 | 95 | 10% | 9.50 | R19(-5) |
| D4 | 91 | 10% | 9.10 | R21(-2), R23(-5), R46(-2) |
| D5 | 75 | 10% | 7.50 | R24(-5), R25(-5), R26(-10), R27(-5) |
| D6 | 100 | 10% | 10.00 | 无 |
| D7 | 100 | 5% | 5.00 | 无 |
| D8 | 100 | 10% | 10.00 | 无 |
| D9 | 100 | 5% | 5.00 | 无（无 `scripts/` 目录，D9 满分） |

> 总分校验：22.00 + 14.25 + 9.50 + 9.10 + 7.50 + 10.00 + 5.00 + 10.00 + 5.00 = **92.35**

## 规则覆盖率

| 统计项 | PASS | FAIL | SKIP | 小计 |
|--------|------|------|------|------|
| 静态规则（29） | 26 | 3 | 0 | 29 |
| 语义规则（22） | 11 | 8 | 3 | 22 |
| 合计（51） | 37 | 11 | 3 | 51 |

- 覆盖率 = 51 / 51 × 100% = **100.00%**
- WARN = 0
- 语义 SKIP：R42（无 scripts）、R44（非 fork 上下文）、R50（无 scripts）

## 质量门禁

| 类型 | 数量 | 说明 |
|------|------|------|
| internal_misbound_rule_or_evidence | 0 | 未发现引用 reviewer 自身而非目标 skill 的条目 |
| low_information_snippet | 0 | 未发现 snippet 无法在目标文件中逐字匹配的条目 |

## 问题列表

### S0 致命缺陷

无。

### S1 重大问题

#### 问题 1：frontmatter 名称与目录名不一致，且包含未知字段
- 命中规则：`R04(S1)`, `R06(S3)`
- 位置：`SKILL.md:2`, `SKILL.md:4`
- 证据：`name: pass-ut-generate`；`license: 完整条款见 LICENSE.txt`
- 问题说明：`name` 与目录 `pypto-pass-ut-generate` 不一致；`license` 不在允许 frontmatter 字段白名单内。
- 修改建议：

```diff
-name: pass-ut-generate
-license: 完整条款见 LICENSE.txt
+name: pypto-pass-ut-generate
```

#### 问题 2：关键操作未给出可执行方法
- 命中规则：`R26(S1)`
- 位置：`SKILL.md:152`
- 证据：`对于错误进行改正。`
- 问题说明：要求“改正错误”，但未说明如何定位错误、如何修改、如何复验，执行链条不完整。
- 修改建议：

```diff
-对于错误进行改正。
+若命令失败，先解析 build_ci 输出中的失败用例名与报错类型；
+按“编译错误/断言失败/超时”分类处理；每次修复后重新执行同一命令，直到退出码为 0。
```

### S2 中等问题

#### 问题 1：流程二大量复述流程一，存在冗余重复
- 命中规则：`R14(S2)`
- 位置：`SKILL.md:171`
- 证据：`同UT生成流程一中步骤1：分析业务`
- 问题说明：流程二的多个步骤仅重复“同流程一步骤X”，信息增量低，降低可读性与执行效率。
- 修改建议：

```diff
-### 步骤 1：分析业务
-    同UT生成流程一中步骤1：分析业务
+### 流程二与流程一差异
+仅保留差异步骤（当前仅步骤4）；其余步骤直接引用“沿用流程一步骤1/2/3/7/8/9”。
```

#### 问题 2：部分引用文件缺少“何时加载”说明
- 命中规则：`R19(S2)`
- 位置：`SKILL.md:212`
- 证据：`| 类别 | 文件路径 |`
- 问题说明：参考资料表列出了路径，但未逐项给出读取时机（如“步骤4构建 function 前读取 program.cpp”）。
- 修改建议：

```diff
-| Opcode 定义 | `pypto/framework/src/interface/operation/opcode.h` |
+| Opcode 定义 | `pypto/framework/src/interface/operation/opcode.h`（步骤6创建 Operation 前读取） |
```

#### 问题 3：多条关键指令缺少“为什么”
- 命中规则：`R23(S2)`
- 位置：`SKILL.md:111`
- 证据：`详细LogicalTensor类信息，请参考：pypto/framework/src/interface/tensor/logical_tensor.h`
- 问题说明：仅给出“参考哪里”，未解释“为什么此时需要参考”，对执行者决策帮助不足。
- 修改建议：

```diff
-详细LogicalTensor类信息，请参考：pypto/framework/src/interface/tensor/logical_tensor.h
+为避免 Tensor 构造参数与 NodeType 选择错误，请在步骤5创建 Tensor 前先阅读：
+pypto/framework/src/interface/tensor/logical_tensor.h
```

#### 问题 4：多个步骤缺少可验证成功标准
- 命中规则：`R24(S2)`
- 位置：`SKILL.md:35`
- 证据：`根据用户描述的业务情况，分析相关业务：`
- 问题说明：步骤1~6大多描述“做什么”，缺少“完成判定”，无法客观验收。
- 修改建议：

```diff
-根据用户描述的业务情况，分析相关业务：
+根据用户描述分析业务，并输出“Pass名称、目标功能、预期图变化、断言点”四项；
+四项齐全即判定步骤1完成。
```

#### 问题 5：命令与路径含未解析占位符
- 命中规则：`R25(S2)`
- 位置：`SKILL.md:42`
- 证据：`在pypto/framework/tests/ut/passes/src/test_xxx.cpp寻找对应的测试文件`
- 问题说明：`test_xxx.cpp`、`xxx.*`、`xxx.xx` 等占位符未给替换规则，直接执行不可落地。
- 修改建议：

```diff
-执行Python3 build_ci.py -c -u=xxx.* -j=24
+将 `xxx` 替换为测试类名（例如 `TestRemoveRedundantOpPass`），执行：
+python3 build_ci.py -c -u=TestRemoveRedundantOpPass.* -j=24 -f=cpp
```

#### 问题 6：缺少全局完成标准定义
- 命中规则：`R27(S2)`
- 位置：`SKILL.md:165`
- 证据：`若覆盖率<=80%，则针对未覆盖的业务，重复以上步骤，进行该Pass UT补充。`
- 问题说明：虽然给出覆盖率阈值，但未定义最终交付条件（如“UT 全部通过 + 覆盖率达标 + 输出清单”）。
- 修改建议：

```diff
-若覆盖率<=80%，则针对未覆盖的业务，重复以上步骤。
+完成标准：
+1) 目标测试类全部通过（退出码0）；
+2) 行覆盖率与方法覆盖率均 >= 80%；
+3) 输出新增用例列表与对应业务点映射。
```

### S3 轻微建议

#### 问题 1：存在模糊措辞，降低指令确定性
- 命中规则：`R21(S3)`
- 位置：`SKILL.md:43`
- 证据：`当未找到对应的测试文件时，可以查找test_xxx.cpp中创建类的名字是否与所要求设计Pass名字是否一致。`
- 问题说明：`可以` 属于弱化表达，建议改为明确动作。
- 修改建议：

```diff
-当未找到对应的测试文件时，可以查找test_xxx.cpp中创建类的名字...
+当未找到对应的测试文件时，立即检查 test_xxx.cpp 中测试类名是否与目标 Pass 名一致。
```

#### 问题 2：无语言标注围栏代码块（位置1）
- 命中规则：`R46(S3)`
- 位置：`SKILL.md:81`
- 证据：`TEST_F(TestRemoveRedundantOpPass, RemoveRedundantOpUTest1) {`
- 问题说明：静态检查在该段围栏处报告“代码块缺少语言标注”。
- 修改建议：

```diff
-```
+```cpp
```

#### 问题 3：无语言标注围栏代码块（位置2）
- 命中规则：`R46(S3)`
- 位置：`SKILL.md:92`
- 证据：`Function(const Program &belongTo, const std::string &funcMagicName, const std::string &funcRawName,`
- 问题说明：静态检查在该段围栏处报告“代码块缺少语言标注”。
- 修改建议：

```diff
-```
+```cpp
```

#### 问题 4：无语言标注围栏代码块（位置3）
- 命中规则：`R46(S3)`
- 位置：`SKILL.md:99`
- 证据：`Program &Program::GetInstance() {`
- 问题说明：静态检查在该段围栏处报告“代码块缺少语言标注”。
- 修改建议：

```diff
-```
+```cpp
```

#### 问题 5：无语言标注围栏代码块（位置4）
- 命中规则：`R46(S3)`
- 位置：`SKILL.md:129`
- 证据：`Operation &Program::AddOperation(const Opcode opCode,`
- 问题说明：静态检查在该段围栏处报告“代码块缺少语言标注”。
- 修改建议：

```diff
-```
+```cpp
```

#### 问题 6：无语言标注围栏代码块（位置5）
- 命中规则：`R46(S3)`
- 位置：`SKILL.md:148`
- 证据：`EXPECT_EQ(assemble_num, kNumZero);`
- 问题说明：静态检查在该段围栏处报告“代码块缺少语言标注”。
- 修改建议：

```diff
-```
+```cpp
```

## 通过规则汇总

| 维度 | 通过规则 |
|------|----------|
| D1 | R01, R02, R03, R05, R07, R08, R09, R10, R44 |
| D2 | R11, R12, R13, R45 |
| D3 | R15, R16, R17, R18 |
| D4 | R20, R22 |
| D5 | 无 |
| D6 | R28, R29, R30, R31 |
| D7 | R32, R33, R51 |
| D8 | R34, R35, R36, R37, R38, R47 |
| D9 | R39, R40, R41 |
| D0 | R43, R43 |
