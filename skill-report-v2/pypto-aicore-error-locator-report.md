# 技能评审报告

## 1. 评审摘要

| 项目 | 结果 |
|------|------|
| 技能名称 | `pypto-aicore-error-locator` |
| 评审时间 | 2026-03-11 |
| 总分 | **97.00 / 100** |
| 等级 | **A** |
| S0 否决 | 否 |
| 规则统计 | PASS 41 / FAIL 7 / WARN 0 / SKIP 3 |

本次评审覆盖 `rules.json` 定义的全部 51 条规则。静态检查脚本执行成功；语义评审完成并通过证据逐字校验。

## 2. 维度评分表

| 维度 | 原始分 (0-100) | 权重 | 加权分 | 扣分明细（FAIL） |
|------|----------------|------|--------|------------------|
| D1 | 98 | 0.25 | 24.50 | R06(S3,-2) |
| D2 | 100 | 0.15 | 15.00 | 无 |
| D3 | 100 | 0.10 | 10.00 | 无 |
| D4 | 96 | 0.10 | 9.60 | R46(S3,-2), R21(S3,-2) |
| D5 | 80 | 0.10 | 8.00 | R24(S2,-5), R25(S2,-5), R26(S1,-10) |
| D6 | 100 | 0.10 | 10.00 | 无 |
| D7 | 98 | 0.05 | 4.90 | R32(S3,-2) |
| D8 | 100 | 0.10 | 10.00 | 无 |
| D9 | 100 | 0.05 | 5.00 | 无（无 `scripts/`，按规范满分） |

**总分校验**：24.50 + 15.00 + 10.00 + 9.60 + 8.00 + 10.00 + 4.90 + 10.00 + 5.00 = **97.00**。

## 3. 规则覆盖率

- 静态规则：29（PASS 27 / FAIL 2）
- 语义规则：22（PASS 14 / FAIL 5 / SKIP 3）
- 总评估数：29 + 22 = **51**
- 覆盖率：51 / 51 × 100% = **100.00%**

## 4. 质量门禁

| 类型 | 数量 | 说明 |
|------|------|------|
| internal_misbound_rule_or_evidence | 0 | 未发现引用 reviewer 自身而非目标 skill 的证据 |
| low_information_snippet | 0 | 所有 FAIL 语义条目的 evidence.snippet 均可在目标文件逐字匹配 |

本次无条目被质量门禁过滤。

## 5. 问题列表

### S0

无。

### S1

#### 问题 1：关键操作缺少可执行方法与验收条件

- 命中规则：`R26(S1)`, `R24(S2)`
- 位置：`SKILL.md:56`
- 证据：`进入用户提供的 pypto 目录路径，重新编译 pypto 包并pip安装。`
- 问题说明：步骤 3 仅描述“重新编译并安装”，未给出具体命令，也未定义如何判定该步骤成功。
- 修改建议（before/after）：

```markdown
修改前：进入用户提供的 pypto 目录路径，重新编译 pypto 包并pip安装。
修改后：在 pypto 根目录执行：
  1) `python3 build_ci.py -f python3 --disable_auto_execute`
  2) `python3 -m pip install dist/*.whl --force-reinstall`
  成功标准：`pip show pypto` 显示新安装版本，且构建命令退出码为 0。
```

### S2

#### 问题 2：执行命令中存在占位符，未给出可直接执行形式

- 命中规则：`R25(S2)`
- 位置：`SKILL.md:72`
- 证据："- 设置 device log 落盘路径：`export ASCEND_PROCESS_LOG_PATH=<用户提供的路径>`"
- 问题说明：`<用户提供的路径>` 属于占位表达，未给出参数替换方式或示例，降低可执行性。
- 修改建议（before/after）：

```markdown
修改前：export ASCEND_PROCESS_LOG_PATH=<用户提供的路径>
修改后：export ASCEND_PROCESS_LOG_PATH="$DEVICE_LOG_PATH"
并在上文明确定义：`DEVICE_LOG_PATH` 为用户输入的绝对路径（示例：`/tmp/aicore_logs`）。
```

### S3

#### 问题 3：frontmatter 使用了未登记字段

- 命中规则：`R06(S3)`
- 位置：`SKILL.md:4`
- 证据：`license: 完整条款见 LICENSE.txt`
- 问题说明：`license` 不在 `known_frontmatter_fields` 白名单中，触发未知字段告警。
- 修改建议（before/after）：

```markdown
修改前：
license: 完整条款见 LICENSE.txt

修改后：
（删除该字段）
```

#### 问题 4：存在未标注语言的围栏代码块

- 命中规则：`R46(S3)`
- 位置：`SKILL.md:23`
- 证据："```"
- 问题说明：示例代码块未声明语言，影响可读性与渲染高亮。
- 修改建议（before/after）：

```markdown
修改前：
```
question:
  ...
```

修改后：
```yaml
question:
  ...
```
```

#### 问题 5：关键判定语句含模糊措辞

- 命中规则：`R21(S3)`
- 位置：`SKILL.md:99`
- 证据："**重要**: 理论上，所有缺失的Uid对应的是同一个`<CCE_ID>`。"
- 问题说明：使用“理论上”会弱化执行标准，导致定位结论不够确定。
- 修改建议（before/after）：

```markdown
修改前：理论上，所有缺失的Uid对应的是同一个`<CCE_ID>`。
修改后：默认判定所有缺失 Uid 对应同一个 `<CCE_ID>`；若比对结果出现多个不同 `<CCE_ID>`，按多个 `<CCE_ID>` 分别输出对应 cpp 文件。
```

#### 问题 6：文档分层不足，缺少“参考深度”层

- 命中规则：`R32(S3)`
- 位置：`SKILL.md:9`
- 证据：`此技能帮助定位测试案例中出现 aicore error 时的问题 CCE 文件。`
- 问题说明：当前结构仅有概述 + 主流程 + 关键点，未将深层参考（如日志格式、命令模板）外置到 `references/`，不满足渐进披露（入口摘要→正文细节→参考深度）。
- 修改建议（before/after）：

```markdown
修改前：所有细节均放在 SKILL.md 主文档中。
修改后：保留主流程在 SKILL.md；将 LEvent 字段说明、grep 模板、常见异常样例移至 `references/aicore-log-patterns.md`，并在流程第 6~7 步标注“需要时加载”。
```

## 6. 通过规则汇总

| 维度 | 通过规则 |
|------|----------|
| D1 | R01, R02, R03, R04, R05, R07, R08, R09, R10, R44 |
| D2 | R11, R12, R13, R14, R45 |
| D3 | R15, R16, R17, R18, R19 |
| D4 | R20, R22, R23 |
| D5 | R27 |
| D6 | R28, R29, R30, R31 |
| D7 | R33, R51 |
| D8 | R34, R35, R36, R37, R38, R47 |
| D9 | R39, R40, R41 |
| D0 | R43, R43 |
