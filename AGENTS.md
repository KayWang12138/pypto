# AGENTS.md

本文件定义跨域通用规则。

## 入口说明

- PyPTO 算子开发统一使用 `pypto-operator-orchestrator`。

## 全局核心原则（必须遵守）

1. 诚实优先于完成度：宁可明确说无法实现，也不要用错误实现假装完成。
2. 任何实现前必须先提供 `docs/api/**` 或 `examples/**` 的证据命中。
3. 出错时先定位最小问题点，再做最小改动，禁止大范围重写。
4. 每一步都必须记录可复现信息（输入、容差、命令、结果、日志、下一步）。
