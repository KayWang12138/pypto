**用户需求**
**需求1**
- 请开发一个pypto算子：
      - 算子名称：sinh
      - 公式：sinh(x) = (e^x - e^(-x)) / 2
- 输入：
      - x:float
- 输出：
      - y:float

**需求2**
- 请开发一个pypto算子：
      - 算子名称：sigmoid
      - 公式：sigmoid(x) = 1 / (1 + e^(-x))
- 输入：
      - x:half
- 输出：
      - y:half