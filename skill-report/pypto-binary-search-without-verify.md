# 技能评审报告

## 评审摘要

| 项目 | 结果 |
|------|------|
| 技能名称 | pypto-binary-search-without-verify |
| 评审时间 | 2026-03-16 |
| 总分 | 93.70 / 100 |
| 等级 | B |
| S0 否决 | 否 |
| 规则统计 | 通过 34 / 失败 10 / 警告 1 / 跳过 4 |

## 维度得分

| 维度 | 名称 | 权重 | 满分 | 得分 | 扣分详情 |
|------|------|------|------|------|---------|
| D1 | Frontmatter 元数据 | 25% | 25.0 | 22.00 | R04(-10): name与目录名不匹配; R06(-2): 未知字段license |
| D2 | 简洁性与效率 | 15% | 15.0 | 15.00 | 无扣分 |
| D3 | 文件结构与导航 | 10% | 10.0 | 9.00 | R18(-5): 引用路径不存在; R19(-5): 被引用文件未说明用途 |
| D4 | 语言与表达 | 10% | 10.0 | 9.30 | R23(-5): 指令缺少原因; R46(-2): 代码块缺少语言标注 |
| D5 | 精确性与可执行性 | 10% | 10.0 | 9.00 | R24(-5): 步骤缺少成功标准; R27(-5): 未定义完成标准 |
| D6 | 工作流完整性 | 10% | 10.0 | 9.50 | R30(-5): 缺少错误处理说明 |
| D7 | 模式与最佳实践 | 5% | 5.0 | 4.90 | R33(-2): 验证依赖LLM判断 |
| D8 | 反模式检测 | 10% | 10.0 | 10.00 | 无扣分 |
| D9 | 脚本与代码质量 | 5% | 5.0 | 5.00 | 无scripts目录，自动满分 |

## 规则覆盖率

| 指标 | 数值 |
|------|------|
| 期望规则数 | 48 |
| 已评估规则数 | 48 |
| 跳过规则数 | 4 |
| 覆盖率 | 100.0% |

**跳过的规则**：R39, R40, R41, R42（原因：不存在 scripts/ 目录，不适用于此技能）

### 规则状态明细

| 规则 | 状态 | 维度 | 严重度 | 类型 |
|------|------|------|--------|------|
| R01 | PASS | D1 | S0 | static |
| R02 | PASS | D1 | S0 | static |
| R03 | PASS | D1 | S0 | static |
| R04 | FAIL | D1 | S1 | static |
| R05 | PASS | D1 | S2 | static |
| R06 | FAIL | D1 | S3 | static |
| R07 | PASS | D1 | S1 | semantic |
| R08 | PASS | D1 | S2 | semantic |
| R09 | PASS | D1 | S2 | semantic |
| R10 | PASS | D1 | S2 | static |
| R11 | PASS | D2 | S1 | static |
| R12 | PASS | D2 | S2 | static |
| R13 | PASS | D2 | S1 | static |
| R14 | PASS | D2 | S2 | semantic |
| R15 | PASS | D3 | S2 | static |
| R16 | PASS | D3 | S2 | static |
| R17 | PASS | D3 | S2 | static |
| R18 | FAIL | D3 | S2 | static |
| R19 | FAIL | D3 | S2 | semantic |
| R20 | PASS | D4 | S2 | semantic |
| R21 | PASS | D4 | S3 | semantic |
| R22 | PASS | D4 | S2 | static |
| R23 | FAIL | D4 | S2 | semantic |
| R24 | FAIL | D5 | S2 | semantic |
| R25 | PASS | D5 | S2 | semantic |
| R26 | PASS | D5 | S1 | semantic |
| R27 | FAIL | D5 | S2 | semantic |
| R28 | PASS | D6 | S1 | semantic |
| R29 | PASS | D6 | S2 | semantic |
| R30 | FAIL | D6 | S2 | semantic |
| R31 | PASS | D6 | S2 | semantic |
| R32 | PASS | D7 | S3 | semantic |
| R33 | FAIL | D7 | S3 | semantic |
| R34 | PASS | D8 | S0 | static |
| R35 | PASS | D8 | S1 | static |
| R36 | PASS | D8 | S1 | static |
| R37 | PASS | D8 | S1 | static |
| R38 | PASS | D8 | S2 | static |
| R39 | SKIP | D9 | S2 | static |
| R40 | SKIP | D9 | S2 | static |
| R41 | SKIP | D9 | S2 | static |
| R42 | SKIP | D9 | S2 | semantic |
| R43 | PASS | D3 | S3 | static |
| R44 | PASS | D1 | S2 | static |
| R45 | PASS | D2 | S2 | static |
| R46 | FAIL | D4 | S3 | semantic |
| R47 | PASS | D7 | S2 | semantic |
| R48 | WARN | D1 | S3 | semantic |

## 质量闸门

| 类型 | 数量 | 说明 |
|------|------|------|
| 内部误绑条目 | 0 | 无 |
| 证据不足条目 | 0 | 无 |

## 问题清单

### S0 致命缺陷

无

### S1 重大问题

#### 问题 1：name 字段与目录名不匹配

**命中规则**：R04 [S1]

> 规则内容：`name` 值应与 skill 目录名一致

**位置**：`SKILL.md:2`

**当前内容**：
> name: pypto-precision-multiple-checkpoints

**问题说明**：
frontmatter 中的 `name` 字段值为 `pypto-precision-multiple-checkpoints`，但该 skill 所在目录名为 `pypto-binary-search-without-verify`，两者不一致。这会导致技能识别和调用时出现混淆。

**修改建议**：
> 将 frontmatter 中的 `name` 字段值修改为 `pypto-binary-search-without-verify`，以与目录名保持一致。
>
> 修改前：`name: pypto-precision-multiple-checkpoints`
> 修改后：`name: pypto-binary-search-without-verify`

---

### S2 中等问题

#### 问题 2：引用路径不存在

**命中规则**：R18 [S2]

> 规则内容：被引用的文件路径必须真实存在

**位置**：`SKILL.md:147`

**当前内容**：
> - PyPTO API: `docs/api/`

**问题说明**：
参考资料章节引用了 `docs/api/` 路径，但该路径在 skill 目录中不存在。用户或 AI 在尝试查阅该文档时会找不到资源。

**修改建议**：
> 创建 `docs/api/` 目录及相应文档，或更新引用路径指向实际存在的文档位置。如果该文档位于项目其他位置，请更新为正确的相对路径。

---

#### 问题 3：被引用文件未说明用途和加载时机

**命中规则**：R19 [S2]

> 规则内容：SKILL.md 应说明每个被引用文件的用途和加载时机

**位置**：`SKILL.md:146-148`

**当前内容**：
> ## 参考资料
>
> - PyPTO API: `docs/api/`

**问题说明**：
引用的 `docs/api/` 文件仅列出路径，未说明该参考文档用于什么目的以及何时需要读取。这会导致使用者不清楚何时需要查阅该文档。

**修改建议**：
> 为参考资料添加描述，说明用途和加载时机。
>
> 修改前：
> ```
> ## 参考资料
>
> - PyPTO API: `docs/api/`
> ```
>
> 修改后：
> ```
> ## 参考资料
>
> - PyPTO API: `docs/api/` - 提供 pypto.assemble、pypto.cast 等 API 的详细用法和函数签名，在需要了解具体 API 参数或返回值时查阅
> ```

---

#### 问题 4：关键指令缺少原因解释

**命中规则**：R23 [S2]

> 规则内容：指令应解释"为什么"，而不仅是"做什么"

**位置**：`SKILL.md:29`

**当前内容**：
> 检查点tensor写在def kernel()里作为输入参数，不要写在 -> 之后

**问题说明**：
这条规则告诉使用者"不要写在 -> 之后"，但未解释原因。使用者可能不理解为什么要遵循这一规则。

**修改建议**：
> 补充原因说明，让使用者理解规则背后的逻辑。
>
> 修改前：`检查点tensor写在def kernel()里作为输入参数，不要写在 -> 之后`
> 修改后：`检查点tensor写在def kernel()里作为输入参数，不要写在 -> 之后，因为检查点需要通过原地修改传递结果，而非通过返回值传递`

---

#### 问题 5：工作流步骤缺少可验证的成功标准

**命中规则**：R24 [S2]

> 规则内容：每个步骤都应具备可验证的成功标准

**位置**：`SKILL.md:20-22`

**当前内容**：
> ### 步骤 1：分析代码结构，确定检查点
>
> 分析kernel和golden代码，确定需要检查的关键计算节点。

**问题说明**：
步骤 1 仅说明要"确定需要检查的关键计算节点"，但未说明如何验证检查点选择是否合理、何时可以认为该步骤完成。

**修改建议**：
> 为每个步骤添加可验证的成功标准。
>
> 修改前：
> ```
> ### 步骤 1：分析代码结构，确定检查点
>
> 分析kernel和golden代码，确定需要检查的关键计算节点。
> ```
>
> 修改后：
> ```
> ### 步骤 1：分析代码结构，确定检查点
>
> 分析kernel和golden代码，确定需要检查的关键计算节点。
>
> **成功标准**：已识别至少 2 个关键计算节点，每个节点的 shape 可从输入/权重推导，且节点位置有明显计算边界（如 matmul、softmax 之后）。
> ```

---

#### 问题 6：未明确定义完成标准

**命中规则**：R27 [S2]

> 规则内容：完成标准必须明确定义

**位置**：`SKILL.md:75`

**当前内容**：
> 然后继续二分进行精度对比，直到找到精度不对的op。

**问题说明**：
skill 说明"直到找到精度不对的op"作为目标，但未说明如何确认找到的 op 就是问题根源，缺少明确的完成判定条件。

**修改建议**：
> 添加明确的完成标准，让使用者知道何时可以确认任务完成。
>
> 修改前：`然后继续二分进行精度对比，直到找到精度不对的op。`
> 修改后：`然后继续二分进行精度对比，直到找到精度不对的op。**完成标准**：已定位到具体的 op，且通过单独测试该 op 可复现精度差异，精度差异可被量化和解释。`

---

#### 问题 7：缺少错误处理或失败恢复说明

**命中规则**：R30 [S2]

> 规则内容：必须包含错误处理或失败恢复说明

**位置**：`SKILL.md:67`

**当前内容**：
> 注意：如果结果相差过大，或者shape不匹配，重新检查添加的检查点代码；device输出为0，可能是卡冲突了或者检查点添加有误。

**问题说明**：
虽然提及了"重新检查添加的检查点代码"，但缺少具体的排查步骤和错误处理流程。当遇到 shape 不匹配、dtype 不一致、测试失败等情况时，使用者可能不知道如何系统性地排查问题。

**修改建议**：
> 扩展错误处理说明，添加具体的排查步骤。
>
> 修改前：
> ```
> 注意：如果结果相差过大，或者shape不匹配，重新检查添加的检查点代码；device输出为0，可能是卡冲突了或者检查点添加有误。
> ```
>
> 修改后：
> ```
> 注意：如果结果相差过大，或者shape不匹配，重新检查添加的检查点代码；device输出为0，可能是卡冲突了或者检查点添加有误。
>
> **错误排查步骤**：
> - 若 shape 不匹配：检查 kernel 和 golden 中检查点变量的 shape 推导过程，确认循环 tile 参数是否正确
> - 若 dtype 不一致：检查是否需要 cast 转换，golden 使用 float32 而 jit 使用 bf16 时需统一
> - 若 device 输出为 0：检查 NPU 卡是否被其他进程占用，或检查检查点 tensor 是否正确初始化
> - 若 assemble 报错：检查输入输出变量名是否相同（不能相同），检查输入输出 dtype 是否一致
> ```

---

### S3 轻微建议

#### 问题 8：frontmatter 包含未知字段

**命中规则**：R06 [S3]

> 规则内容：frontmatter 应仅包含已知字段（name, description, license, compatibility, metadata）

**位置**：`SKILL.md:5`

**当前内容**：
> license: 完整条款见 LICENSE.txt

**问题说明**：
`license` 字段不在已知字段列表（name, description, license, compatibility, metadata）的标准定义中。根据 rules.json，`license` 实际上是已知字段之一，但该字段值"完整条款见 LICENSE.txt"不是标准的 license 声明格式。

**修改建议**：
> 如果需要保留 license 信息，建议使用标准格式如 `MIT`、`Apache-2.0` 等，或将详细信息移至 `metadata` 字段中。

---

#### 问题 9：验证任务依赖 LLM 判断

**命中规则**：R33 [S3]

> 规则内容：验证逻辑应优先使用确定性脚本

**位置**：`SKILL.md:69-71`

**当前内容**：
> ### 步骤 5：运行测试并分析结果
>
> 根据对比结果，定位到第一个精度不匹配的检查点，问题就在该检查点之前或该检查点的计算中。

**问题说明**：
"运行测试并分析结果"步骤依赖人工或 LLM 分析精度对比结果，缺少自动化验证脚本。虽然对于精度调试场景完全自动化较困难，但可以考虑提供辅助工具。

**修改建议**：
> 考虑在 scripts/ 目录中添加自动化精度对比工具，减少对 LLM 判断的依赖。例如，提供一个自动比较检查点 tensor 并输出差异报告的脚本。

---

#### 问题 10：代码块引用缺少语言标注

**命中规则**：R46 [S3]

> 规则内容：代码块应带有语言标注，纯文本块除外

**位置**：`SKILL.md:147`

**当前内容**：
> - PyPTO API: `docs/api/`

**问题说明**：
该处使用行内代码块引用路径，属于纯文本引用，无需语言标注。但如果后续添加代码示例，应使用带语言标注的代码块。

**修改建议**：
> 当前为路径引用，符合纯文本块例外情况。若后续添加代码示例，请使用带语言标注的代码块，如：
> ```python
> # 示例代码
> checkpoint = torch.empty((batch, hidden_dim), dtype=torch.float32)
> ```

---

#### 问题 11：description 扩展触发范围不足

**命中规则**：R48 [WARN]

> 规则内容：description 应该 pushy 一些，避免在技能本应发挥作用的场景下却不使用

**位置**：`SKILL.md:3`

**当前内容**：
> 适用于算子运行通过但精度不满足要求、需要定位具体op导致精度问题、需要对比多个中间结果的场景。

**问题说明**：
description 仅列出显式触发场景（"算子运行通过但精度不满足要求"等），未使用扩展触发短语（如 "whenever"、"including"、"or any related to"）来覆盖更多相关场景。这可能导致在某些应该使用该技能的场景下未被触发。

**修改建议**：
> 考虑扩展触发范围，使用更具包容性的表述。
>
> 修改前：
> `适用于算子运行通过但精度不满足要求、需要定位具体op导致精度问题、需要对比多个中间结果的场景。`
>
> 修改后：
> `适用于算子精度调试场景，包括但不限于精度不满足要求、中间结果对比、op级精度定位等任何与精度相关的调试需求。当遇到任何精度相关问题时，均可使用此技能进行诊断。`

---

## 通过项

共 34 条规则通过。

| 维度 | 通过规则 |
|------|---------|
| D1 | R01, R02, R03, R05, R07, R08, R09, R10, R44 |
| D2 | R11, R12, R13, R14, R45 |
| D3 | R15, R16, R17, R43 |
| D4 | R20, R21, R22 |
| D5 | R25, R26 |
| D6 | R28, R29, R31 |
| D7 | R32, R47 |
| D8 | R34, R35, R36, R37, R38 |
| D9 | (无scripts目录，自动满分) |
