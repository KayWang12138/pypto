# pypto.SymbolicScalar Introduction

SymbolicScalar is used to implement dynamic expressions of data, such as the actual Shape passed in to a Tensor at runtime, the actual values of a Tensor at runtime, and arithmetic expressions derived from runtime data.

