# pypto.index\_add

## Function Prototype

```python
index_add(input: Tensor, dim: int, index: Tensor, source: Tensor, *, alpha: Union[int, float] = 1) -> Tensor
```

The non-inplace version of pypto.index_add_. See [pypto.index_add_](pypto-index_add_.md).

