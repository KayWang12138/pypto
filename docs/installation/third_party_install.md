# Pypto 三方依赖安装 # 
如果无法访问[]()，按照如下步骤，安装三方依赖
```
mkdir -p <path-to-your-thirdparty>
cd <pypto-source-code>/tools

bash download_thirdparty.sh <path-to-your-thirdpary>

export PYPTO_THIRDPARTY_PATH= *absolute path for <path-to-your-thirdpary>*
```