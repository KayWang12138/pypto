# 性能调优迭代模板与详细参考

> 本文件从主 SKILL.md 提取的详细模板、流程图和示例，主 SKILL.md 仅保留简要指引和引用链接。

---

## 1. 状态映射：编排器状态 ↔ 主技能步骤

**第一级：主流程状态 ↔ 主技能步骤**

| 编排器状态 | 对应主技能步骤 | 具体内容 | 编排器完成条件 |
|-----------|--------------|---------|--------------|
| INIT | 步骤 0：确定调优目标 | 询问用户目标、计算目标值、设置终止条件 | 用户确认目标性能值 |
| S1_SETUP | 步骤 1：精度校验 | 设置环境、编译、执行用例、精度校验（1.1-1.4） | 环境OK + 精度通过 |
| S2_COLLECT | 步骤 2：性能数据采集 | 启用 debug_options、运行用例、确认数据文件（2.1-2.3） | swimlane.json 存在 |
| S3_ANALYZE | 步骤 3：性能数据分析 | 加载 perf-analyzer、生成报告、建立基准（3.1-3.3） | 性能报告文件存在 |
| S4_TUNE | 步骤 4：分步骤调优 | 见下方第二级映射 | 3个PHASE全部完成 |
| S5_REPORT | 步骤 5：生成报告 | 输出最终看板、填充报告、保存（5.1） | 报告已保存 + debug_options已还原 |

**第二级：S4_TUNE 子阶段 ↔ 步骤 4 各子步骤**

| 编排器 PHASE | 对应步骤 4 子步骤 | 加载的子技能 | 优化内容 | 退出条件 |
|-------------|----------------|------------|---------|---------|
| PHASE_FRONTEND | 步骤 4.0 第1步 + 步骤 4.1 第1段 + 步骤 4.2 开箱部分 + 步骤 4.3 迭代循环 | tune-frontend | 代码写法、TileShape、BLOCK_SIZE、基础 runtime_options | 达标 或 连续5轮无提升 |
| PHASE_SWIMLANE | 步骤 4.0 第2步 + 步骤 4.1 第2段 + 步骤 4.2 决策树 + 步骤 4.3 迭代循环 | tune-swimlane | Stitch、合图、L1Reuse、调度策略、轴切块 | 达标 或 连续8轮无提升 |
| PHASE_INCORE | 步骤 4.0 第3步 + 步骤 4.1 第3段 + 步骤 4.3 迭代循环 | tune-incore | 指令级优化、核内流水、特殊Shape | 达标 或 达到理论上限 |

**第三级：迭代轮次子状态 ↔ 步骤 4.3 迭代调优流程中的各环节**

| 编排器迭代子状态 | 对应步骤 4.3 环节 | 具体操作 |
|----------------|----------------|---------|
| ITER_START | 环节 1：选择优化点 | 根据决策树（步骤 4.2）或子技能指南选择一个优化点 |
| ITER_MODIFY | 环节 2：修改代码 | 每次只修改一个参数 |
| ITER_VERIFY | 环节 3：验证精度 | 运行测试用例，按步骤 1.4 的检查流程验证 |
| ITER_MEASURE | 环节 4：测试性能 | 采集性能数据，对比基准性能，计算提升百分比 |
| ITER_RECORD | 环节 5：记录结果 | 更新调优记录表（步骤 4.3 的更新示例） |
| ITER_JUDGE | 环节 6：检查终止条件 | 判断是否达标 / 连续无提升次数是否达阈值 |
| ITER_ROLLBACK | 环节 3 失败分支 | 回退代码修改，按步骤 4.4 异常处理记录 |

**阶段交接 ↔ 步骤 4.5**

| 编排器强制动作 | 对应主技能章节 | 说明 |
|-------------|-------------|------|
| PHASE 退出时生成摘要 | 步骤 4.5.1：阶段交接摘要模板 | 按 7 节模板生成完整摘要 |
| ⛔ 生成摘要后立即执行 /compact | 步骤 4.5.2：上下文压缩规则 | 强制门控！清除冗余上下文 |
| 交接并进入下一阶段 | 步骤 4.5.3：阶段交接流程 | 按流程图校验→确认→加载新子技能 |
| 校验摘要完整性 | 步骤 4.5.3 末尾：摘要校验清单 | 逐项确认 5 个检查项 |

---

## 2. 调优进度模板与状态看板

**调优进度模板**：
```markdown
## 📊 性能调优进度

### 目标
- 算子: [算子名称]
- 基准: [基准性能] us
- 目标: [目标性能] us (提升X倍)

### 进度
- ✅ 精度校验通过
- 🔄 [当前阶段]
- ⏸️ [待优化阶段]

### 性能记录
| 轮次 | 优化内容 | 执行时间 | 提升 |
|------|---------|---------|------|
| 基准 | - | XX us | - |
```

**更新时机**：
- 每次优化后（无论成功失败）
- 阶段切换时
- 性能提升>5%时
- 连续 3 次无提升时

**状态看板**（每完成 5 轮优化输出）：
```markdown
## 当前状态
- 性能: XX us (累计提升 XX%)
- 进度: ████████░░ XX%
- 成功率: X/Y 轮
```

---

## 3. 迭代调优流程（步骤 4.3）

```
┌───────────────────────────────────────┐
│     迭代调优流程（每次循环）          │
├───────────────────────────────────────┤
│                                       │
│  1. 选择一个优化点                    │
│     └─ 根据决策树或子技能指南         │
│                                       │
│  2. 修改代码                          │
│     └─ 每次只修改一个参数             │
│                                       │
│  3. 验证精度 ⭐                       │
│     ├─ 运行测试用例                   │
│     ├─ 验证用例是否通过               │
│     └─ 失败，尝试解决，不行则回退修改 │
│                                       │
│  4. 测试性能                          │
│     ├─ 采集性能数据                   │
│     ├─ 对比基准性能                   │
│     └─ 计算提升百分比                 │
│                                       │
│  5. 记录结果 & 更新进度 ⭐            │
│     ├─ 性能提升：保留修改             │
│     ├─ 性能下降：回退修改             │
│     └─ 更新调优记录表                  │
│        ├─ 成功: 标记✅并记录          │
│        └─ 失败: 标记❌并说明          │
│                                       │
│  6. 检查终止条件                      │
│     └─ 达到目标或无法提升则停止       │
│                                       │
│  7. 状态看板（每 5 轮输出）             │
│     └─ 展示当前进度和性能趋势         │
│                                       │
└───────────────────────────────────────┘
```

**⚠️ 重要：每次修改都要验证精度，不要怕麻烦！**

**⚠️ 精度验证失败处理**：
- **修改导致失败**：尝试解决，不行则回退修改，记录失败原因，尝试其他优化方案
- **不要尝试修复精度问题**，精度问题是算子实现问题，不是调优能解决的

**关键原则**：
1. 每次只修改一个优化点
2. 修改后立即测试性能和精度
3. 精度失败，尝试解决，不行的话，则回退修改
4. 性能回退则尝试其他优化点
5. **每个阶段独立迭代**，完成后再进入下一阶段
6. 三个阶段顺序执行：开箱调优 → 深度调优 → 核内调优
7. 当长时间达成调优目标，或者没有调优空间时，可以重新审视一下算子实现方式，重新设计开发算子

**调优记录更新示例**：
```markdown
### 性能记录（持续更新）
| 轮次 | 优化内容 | 执行时间(us) | 变化 | 状态 |
|------|---------|-------------|------|------|
| 基准 | - | 79.34 | - | ✅ |
| 1 | BLOCK_SIZE_KV 128→64 | 68.54 | -13.6% | ✅ |
| 2 | unroll_list [8,4,2,1] | 74.44 | +8.6% | ❌回退 |
| 3 | cube_nbuffer {0:4} | 66.14 | -3.5% | ✅ |

### 当前进度
- 性能: 66.14 us (累计提升 16.6%)
- 进度条: ████████░░░░░░░░ 42%
- 成功率: 2/3 轮
```

**状态看板示例**（每 5 轮输出）：
```markdown
## 📊 调优状态看板

当前阶段: 深度性能调优
性能: 66.14 us | 累计提升: 16.6% | 目标: 39.67 us
进度: ████████░░░░░░░░ 42%

### 优化统计
- 总轮次: 10 轮
- 成功: 4 轮 (40%)
- 失败: 6 轮 (60%)
- 最佳优化: 增加并行度 (+13.6%)

### 当前瓶颈
1. ⭐⭐⭐ AIC核心利用率低 (13.63%)
2. ⭐⭐⭐ 气泡率高 (59.58%)
```

**阶段间衔接**：每个阶段完成后，必须执行步骤 4.5（阶段摘要与上下文压缩），再进入下一阶段。
当长时间达成调优目标，或者没有调优空间时，可以重新审视一下算子实现方式，重新设计开发算子。

---

## 4. 异常情况处理（步骤 4.4）

**连续 3 次优化无提升**：
```markdown
## ⚠️ 调优进展停滞

已连续 3 次优化无提升，可能原因：
1. 已达性能上限
2. 当前优化方向不佳
3. 需要算法层面改进

建议：
- 继续尝试其他优化
- 接受当前性能
- 重新设计算子
```

**精度验证失败**：
```markdown
## ❌ 精度验证失败

- 失败轮次: 第X轮
- 当前优化: [优化内容]
- 处理: 尝试解决，但未生效，回退修改

调优记录更新: 标记当前优化为❌
```

---

## 5. 阶段交接摘要模板（步骤 4.5.1）

**⛔ 强制要求：阶段退出条件满足后，必须按以下模板生成完整摘要，不得遗漏任何一节。**

````markdown
## 📋 阶段交接摘要：[开箱调优 / 深度调优 / 核内调优] → [下一阶段名称]

### 1. 算子当前性能状态
- 算子名称: [名称]
- 算子文件: [impl文件路径]
- 基准执行时间: [初始值] us
- 当前执行时间: [当前值] us
- 累计性能提升: [百分比]%
- 核心利用率: [当前值]%
- 气泡率: [当前值]%
- 负载均衡度: [当前值]%
- 调优目标: [目标值] us（当前差距: [差值]%）
- 目标达成状态: ✅已达标 / ❌未达标

### 2. 已采纳优化（代码中已生效）
| # | 优化项 | 修改内容 | 性能收益 | 代码位置 |
|---|--------|---------|---------|---------|
| 1 | [优化名称] | [具体修改] | [收益百分比] | [文件:行号] |
| 2 | ... | ... | ... | ... |

### 3. 已失败优化（避免重试）
| # | 尝试项 | 失败原因 | 是否已回退 | 备注 |
|---|--------|---------|-----------|------|
| 1 | [优化名称] | [精度失败/OOM/性能回退] | ✅已回退 | [补充说明] |
| 2 | ... | ... | ... | ... |

### 4. 约束与发现（关键！后续阶段必须遵守）
- [约束1]: [描述，如"输出tensor不能inplace reshape后切片写入"]
- [约束2]: [描述，如"stitch_max_num=256时workspace溢出，最大可用128"]
- [发现1]: [描述，如"瓶颈已从调度开销转向核内AIC_0的task3"]
- ...

### 5. 当前关键代码配置
```python
# 当前 jit 装饰器配置
@pypto.frontend.jit(
    runtime_options={...},
    pass_options={...},
    debug_options={"runtime_debug_mode": 1}
)

# 当前关键参数
BLOCK_SIZE = [当前值]
TileShape配置 = [当前值]
# ... 其他影响性能的关键常量
```

### 6. 下一阶段建议
- 当前主要瓶颈: [描述，如"AIC_0 task3 matmul耗时占比40%"]
- 建议优化方向: [描述，如"小Shape预处理 或 enable_split_k"]
- 需要关注的性能数据文件: [路径]

### 7. 性能趋势摘要（从本轮次性能记录中浓缩）
- 起始性能: [值] us → 结束性能: [值] us（本阶段提升: [%]）
- 总迭代轮次: [N]轮，成功: [X]轮，失败: [Y]轮
- 最佳优化: [优化项]（+[%]）
- 性能曲线趋势: [持续下降 / 先降后平 / 波动无提升]
````

---

## 6. 上下文压缩规则（步骤 4.5.2）

**⛔ 摘要生成后，必须立即执行 `/compact` 压缩上下文。这不是可选操作！**

`/compact` 的作用是清除对话历史中的冗余信息，保留阶段交接摘要作为后续工作的核心上下文。没有这一步，后续阶段的模型有效注意力会严重衰减。

`/compact` 执行前后，遵循以下压缩规则：

| 信息类别 | 保留策略 | 说明 |
|---------|---------|------|
| 已采纳优化 | ✅ 完整保留 | 包含具体修改和代码位置 |
| 已失败优化 | ✅ 完整保留 | 避免重复尝试 |
| 约束与发现 | ✅ 完整保留 | 后续阶段必须遵守 |
| 当前代码配置 | ✅ 完整保留 | 需基于最新代码继续修改 |
| 性能趋势 | ✅ 浓缩保留 | 只保留趋势摘要，不保留每轮详细日志 |
| 中间调试日志 | ❌ 丢弃 | 如精度失败时的完整报错堆栈 |
| 失败代码完整内容 | ❌ 丢弃 | 已回退的代码不再需要 |
| 冗余性能对比细节 | ❌ 丢弃 | 只保留最终结果 |
| 子技能完整内容 | ❌ 不重新加载 | 下一阶段加载新的子技能 |

---

## 7. 阶段交接流程（步骤 4.5.3）

**⛔ 三步强制流程，缺一不可：**

```
当前阶段退出条件满足
    ↓
⛔ 步骤1: 强制生成阶段交接摘要（4.5.1 模板）
    ↓
✅ 步骤2: 校验摘要完整性
    ├─ 已采纳优化列表非空？（或明确标注"无"）
    ├─ 已失败优化列表非空？（或明确标注"无"）
    ├─ 约束与发现非空？（或明确标注"无新约束"）
    └─ 关键代码配置已包含？
    ↓
📋 输出摘要给用户确认
    ↓
⛔ 步骤3: 执行 /compact 压缩上下文
    ├─ 这一步是强制门控，不是可选操作！
    ├─ /compact 会清除迭代过程中的冗余信息（调试日志、失败代码、重复输出等）
    └─ 压缩后，仅以阶段交接摘要为核心上下文继续工作
    ↓
🔄 步骤4: 进入下一阶段
    ├─ 以阶段交接摘要为上下文基础
    ├─ 加载新子技能 SKILL.md
    ├─ 基于摘要中的"下一阶段建议"确定首个优化方向
    └─ 开始新一轮迭代调优
```

**⛔ 禁止事项**：
- ❌ 只生成摘要不执行 `/compact`（上下文仍然膨胀，后续阶段质量退化）
- ❌ 跳过摘要直接 `/compact`（关键信息丢失）
- ❌ `/compact` 后不确认摘要内容就继续（可能遗漏关键约束）

**⚠️ 摘要校验清单（生成后必须逐项确认，确认后立即执行 /compact）**：
- [ ] 性能数据是否与实际一致？（执行时间、利用率、气泡率）
- [ ] 已采纳优化是否遗漏？（核对口诀中的 ✅ 项）
- [ ] 已失败优化是否遗漏？（核对口诀中的 ❌ 项）
- [ ] 约束与发现是否遗漏？（检查调优过程中的关键发现）
- [ ] 代码配置是否是最新的？（包含所有已采纳的修改）
- [ ] ⛔ 校验通过后，是否已执行 `/compact`？（未执行禁止进入下一阶段）

---

## 8. 报告模板（步骤 5.1）

**⚠️ 重要：调优结束后，必须生成调优报告！**

**操作步骤**：

1. **输出最终状态看板**：
```markdown
## 📊 最终调优状态

### 目标达成
- 目标: 提升 1 倍 (XX → XX us)
- 实际: 提升XX% (XX → XX us)
- 达成率: XX%
- 状态: ✅达标 / ❌未达标

### 优化总结
- 总轮次: X轮
- 成功率: X%
- 耗时: XX分钟
- 最佳优化: [优化项] (+XX%)

### 性能趋势
基准 → 最终: XX us → XX us
```

2. 根据实际调优过程填充模板内容
   - 调优概述：算子名称、调优目标、实际达成、调优时长、调优轮次
   - 性能对比：原始性能、最终性能、提升百分比
   - 关键优化：列出所有有效的优化及提升比例
   - 最佳配置：最终优化配置代码
   - 调优记录：每轮调优的详细记录

3. 要详细记录调优过程中跳过的出错问题，便于开发者后续解决

4. 保存调优报告

**报告示例**：
```markdown
# Flash Attention Score 性能调优报告

## 调优概述
- 算子名称: Flash Attention Score
- 调优目标: 提升 5 倍
- 实际达成: 提升 5.89 倍 (83.0%)
- 调优时长: 73 分钟
- 调优轮次: 11 轮

## 性能对比
| 指标 | 原始性能 | 最终性能 | 提升 |
|------|---------|----------|------|
| 执行时间 (us) | 27469.66 | 4665.66 | 83.0% |
| 核心利用率 | 46% | 85% | +39% |
| 气泡率 | 54% | 15% | -39% |

**性能倍数**: 5.89 倍

## 问题记录
```

---

## 9. 常见错误

### 错误 1：跳过子技能直接调优

**错误表现**：
- 完成步骤3（性能数据分析）后，直接开始尝试优化
- 没有加载对应的子技能获取详细指南
- 凭经验或猜测进行调优

**正确做法**：
1. 根据性能分析报告判断调优方向（参考步骤 4.2 决策流程）
2. 读取对应子技能的 SKILL.md 文件
3. 按照子技能中的详细指南执行调优
4. 严格执行"修改一处 → 测试 → 验证"的迭代流程

### 错误 2：一次性修改多处优化点

**错误表现**：
- 同时修改多个配置参数
- 全部修改完才测试性能

**正确做法**：
- 每次只修改一个优化点
- 修改后立即测试
- 验证精度和性能
- 记录结果后再尝试下一个优化点

### 错误 3：不验证精度

**错误表现**：
- 修改代码后直接测试性能，不验证精度
- 认为小改动不会影响精度

**正确做法**：
- **每次修改都要验证精度，不要怕麻烦！**
- 即使是小改动，也要验证精度
- 精度失败，不要立即回退，要进行简单分析尝试后，如果还是不能解决，则回退
