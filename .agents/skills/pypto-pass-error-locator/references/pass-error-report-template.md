## PyPTO Pass 问题分析报告

### 一、基本信息
- **问题标题**: <一句话概述问题>
- **错误级别**: <ERROR/WARN/INFO>
- **发生时间**: <时间戳>
- **进程ID**: <PID>
- **复现命令**: `<用户提供的复现命令>`
- **用户代码路径**: `<相关脚本/测试用例路径>`
- **环境信息**:
  - PyPTO版本: <版本>
  - Python版本: <版本>
  - 机器/设备信息: <NPU型号/环境信息>
  - 关键环境变量:
    - `ASCEND_PROCESS_LOG_PATH=<值>`
    - `ASCEND_GLOBAL_LOG_LEVEL=<值>`
- **是否成功复现**: <是/否>
- **是否开启调试模式**: <是/否>
- **相关输出目录**:
  - 日志目录: `<logs/...>`
  - 图编译输出目录: `<output/...>`

### 二、错误位置
- **源码文件**: `<文件路径>`
- **代码行号**: `<行号>`
- **Pass模块**: `<Pass名称>`
- **Element类型**: `<Config/Function/Operation/其他>`
- **相关函数/接口**: `<函数名或入口>`
- **定位依据**:
  - 日志定位: `<日志中的文件:行号>`
  - 代码上下文: `<触发条件 / 关键分支变量 / 返回码>`

### 三、错误信息
#### 3.1 原始错误日志
```text
<原始日志，保留完整错误文本>
```

#### 3.2 日志字段解析
- **日志级别**: <ERROR/WARN/INFO>
- **模块名及进程**: <如 PYPTO(638465)>
- **日志时间**: <时间戳>
- **文件及行号**: `<file:line>`
- **所属模块类型**: <PASS/其他>
- **模块信息**: `<如 NBufferMerge.Config>`
- **错误内容**: <提取后的核心报错信息>

#### 3.3 关联日志/上下文
```text
<多行错误上下文、WARN、堆栈、相邻日志>
```

### 四、错误原因分析
#### 4.1 异常分类
- **异常类型**: <参数配置异常 / 算子约束违反 / 段错误 / 属性缺失 / 计算图成环 / 索引越界 / 其他>
- **分类依据**: <对应日志特征、关键字段、代码行为>

#### 4.2 业务场景说明
- <简述用户测试场景、输入条件、目标业务逻辑>

#### 4.3 证据分析
1. **日志证据**
   - <日志片段1 + 说明>
   - <日志片段2 + 说明>

2. **源码证据**
   - 文件: `<文件路径>`
   - 行号范围: `<start-end>`
   - 触发条件: <条件表达式/前置状态>
   - 关键分支变量: <变量名=值>
   - 返回码/异常点: <返回码或抛异常位置>

```cpp
<关键源码片段，标注文件名和行号>
```

3. **计算图/IR证据**（如适用）
- 当前Pass Before图: `<路径>`
- 当前Pass After图: `<路径或缺失说明>`
- 上游相邻Pass输出: `<路径>`
- 状态异常描述: <图结构变化、IR异常、After缺失等>

4. **规则/约束证据**
- 参考文档: `<文档路径或链接>`
- 违反规则: <约束内容>
- 规则说明: <该错误为何违反约束>

#### 4.4 根因链
> 必须完整描述：现象 -> 触发位置 -> 状态异常 -> 违反规则

- **现象（日志）**: <看到的外部报错/中断现象>
- **触发位置（源码切片）**: <具体文件、行号、函数、代码片段>
- **状态异常（计算图/IR变化）**: <Before/After差异、属性缺失、图结构异常等>
- **违反规则（约束文档）**: <具体规则、约束条目、接口要求>

#### 4.5 特殊说明：初始无异常日志场景（如适用）
- **首次外显现象**: <pass中断且After图缺失等>
- **补充诊断手段**: <增加异常捕获/临时日志/DumpJsonFile>
- **最终异常证据**: <补充捕获后新增的错误码、错误文本、源码行号>

#### 4.6 影响范围
- **直接影响**: <当前Pass/当前测试用例/当前业务流>
- **潜在影响**: <上下游Pass/其他场景/其他算子>

### 五、修复建议
#### 5.1 修复方案
- <给出明确、具体、可执行的修复方向>

#### 5.2 具体步骤
1. <步骤1：修改哪个文件、哪个逻辑>
2. <步骤2：增加什么校验/判空/映射修正>
3. <步骤3：如何重新编译安装与复现验证>
4. <步骤4：如何确认修复生效>

#### 5.3 验证方式
- <验证命令>
- <预期日志/图/IR结果>
- <回归范围>

#### 5.4 风险提示
- <风险1>
- <风险2>
- <是否需要同步检查上下游模块>

### 六、附录
- **完整日志路径**: `<日志文件路径>`
- **相关源码文件**:
  - `<文件1>`
  - `<文件2>`
- **计算图/IR文件**:
  - `<Before图路径>`
  - `<After图路径>`
  - `<IR路径>`
- **相关文档**:
  - `references/pass-error-analysis-guide.md`
  - `references/ir-analysis-guide.md`
  - `<其他约束文档>`
- **参考案例**: `<类似问题案例链接或路径>`

### 七、输出校验
- [ ] 已包含六个必需章节：基本信息、错误位置、错误信息、原因分析、修复建议、附录
- [ ] 根因链完整：现象 -> 触发位置 -> 状态异常 -> 违反规则
- [ ] 每个环节均有证据支撑：日志 / 源码 / 计算图或IR / 约束文档
- [ ] 代码位置已精确到文件和行号
- [ ] 修复建议为具体操作步骤，不是泛化描述

### 八、回退记录（如本次分析过程中发生）
- **回退原因**: <格式不完整 / 证据链断裂 / 修复建议不可执行 / 其他>
- **缺失内容**: <缺失的日志、图、IR、规则或结论>
- **补充动作**: <重新获取日志 / 补打计算图 / 重新分类 / 重新分析>
- **回退到步骤**: <步骤2 / 步骤4 / 步骤5>
